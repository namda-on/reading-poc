# CLAUDE.md

## Project overview

리스닝이 불가능한 환경에서도 같은 회화 콘텐츠로 훈련하는 **리딩 모드 POC**.
리스닝의 "실시간 처리"를 소리 없이 재현한다 — 문장 크기만큼 확보된 말풍선 위를 N개짜리 창(window)이 청크의 실제 위치를 따라 슬라이딩하며 노출하고, 지나간/미래 청크는 빈 공백으로 남겨 되돌아가기·미리보기를 차단한다. 대화가 끝나면 한국어 객관식 1문항을 바텀시트로 푼다.

세션 시작 화면에서 **리딩 / 리스닝 / 전광판 / 고정** 모드를 고른다. 리스닝은 같은 대화를 실제 TTS 음성(speech.epop.ai)으로 재생하되 텍스트는 완전히 숨긴다(순수 듣기). 전광판은 같은 말풍선 레이아웃에서 각 문장이 오른쪽→왼쪽으로 흘러 지나가는 LED 전광판식 노출이다. 고정은 시선을 한 곳에 둔 채 화면 중앙에서 단어/청크가 제자리 교체되는 RSVP식 노출이다(시선 이동 없이 읽기 비교용). 모든 모드가 끝나면 같은 퀴즈로 이어진다.

**주관적 체험용 프로토타입**이다. 정량 로깅·서버·백엔드 없음. 실험 축은 노출 단위(단어/청크)·창 크기 N·속도(ms/음절).

설계·구현 계획 문서: `docs/superpowers/specs/`, `docs/superpowers/plans/`.

## Commands

```bash
npm run dev            # 개발 서버
npm run build          # tsc -b && vite build
npm test               # vitest (chunk / reveal / useSlidingReveal)
python3 scripts/extract.py   # 대화 데이터(dialogs.json) 재생성
```

## Directory map

- `src/lib/chunk.ts` — 문장 → 청크 분할(순수 함수). 단어 모드 / 규칙 모드.
- `src/lib/reveal.ts` — 노출 스케줄(순수 함수). 음절 기반 dwell 계산.
- `src/hooks/useSlidingReveal.ts` — 스케줄을 타이머로 구동(generation guard).
- `src/components/` — `StoryList`(첫 화면) · `TopicList` · `SessionStart`(모드 선택 시작 화면) · `ReadingSession`(리딩 재생) · `ListeningSession`(리스닝 오디오 재생, 텍스트 숨김) · `MarqueeSession`(전광판식 가로 흐름) · `FixedSession`(시선 고정 RSVP) · `DialogBubble` · `SettingsPanel` · `QuizSheet`(문제 바텀시트) · `Quiz`(객관식)/`ArrangeQuiz`(단어 배열)/`DictationQuiz`(받아쓰기).
- `src/settings/SettingsContext.tsx` — 설정 상태 + localStorage.
- `src/lib/progress.ts` — 푼 토픽 localStorage 저장.
- `src/lib/keySentence.ts` — 배열·받아쓰기 문제에 쓸 '핵심 1문장' 선택. 스크립트를 문장 단위로 쪼개(항상 단일 문장), 퀴즈 힌트(질문·정답·해설)와 한국어 글자 바이그램 겹침이 가장 큰 문장을 고른다(정답 근거 문장). 힌트/겹침이 없으면 3~9단어 중 가장 긴 문장.
- `src/lib/quizHint.ts` — `topicSeq`의 퀴즈 질문·정답·해설을 합친 한국어 힌트(핵심 문장 선정용).
- `src/data/` — `dialogs.json`(생성물, 커밋됨) · `quizzes.json`(수기 저작) · `types.ts` · `quizTypes.ts`.
- `scripts/extract.py` — 외부 sqlite에서 대화 추출.
- `scripts/build_vocab_expression.py` — **어휘→표현** 프로토타입 데이터 생성(레포 밖 CSV → `public/vocab-expression.data.json`). 소스는 `VE_EXPR_CSV`(기본 `유기적 통합모드 문장 (공유용) - 문장 제출(0827).csv`)·`VE_VOCAB_CSV`(`All_*.csv`)·`VE_GSE_CSV`(`gse_corrected_final_0624 - 보정결과_전체.csv`) 환경변수. 표현 CSV는 **그룹(id)당** 트리거 어휘(`trigger`,`trigger_dictseq`) + 표현 패턴(`표제어 또는 회화패턴`) + 문장 3~6개(`문장1`=트리거가 들어간 대표 문장) + 결과 카드(`카드 초록(형태)`/`회색1행(뜻)`/`회색2행(설명)`) 구조이며, 그룹 첫 행에만 값이 있어 **forward-fill**로 읽는다. `trigger_dictseq`로 어휘 CSV(`seq`)와 gse(`dictSeq`)에 조인해 어휘 예문·뜻·품사·CEFR·레벨을 붙인다. 정렬은 `trigger_newRank3`(빈도) 오름차순, 문항 수는 `VE_MAX_ITEMS`(**기본 0 = 전체**, 조건 통과 222그룹 전부를 굽는다. 세션을 짧게 줄이려면 숫자를 준다).
  **한 문항에 두 버전의 어휘 문장을 함께 굽는다**(같은 문항을 두 방식으로 비교해야 하므로 양쪽 모두 성립하는 그룹만 사용):
  - `vocabA` — 어휘 CSV의 `learnSentence`(표현 문장과 **다른** 문장). 막아야 하는 건 **여러 단어 표제어를 일부만 괄호친 예문**(`baggage claim`을 배우는데 빈칸이 `[baggage]`)이고, 단일 단어 표제어의 빈칸은 굴절형이어도(`child`→`[children]`) 인정한다. 복합어가 빈칸 두 개로 쪼개진 경우(`[ice] [cream]`)는 `merge_blanks`가 하나로 잇는데, **합친 결과가 학습 어휘를 가리킬 때만** 합친다 — `[went] [to]`는 `went to`가 되어 어휘(`go`)와 무관하므로 합치지 않고 그대로 제외한다(UI가 첫 빈칸만 입력칸으로 만들어 나머지가 대괄호로 남으므로 빈칸이 둘 이상이면 버린다).
  - `vocabB` — **표현 문장(문장1)에서 트리거 어휘를 빈칸으로** 만든 것(`blank_trigger`). 굴절형도 인정하므로 정답은 문장에 쓰인 표면형(`parent`→`parents`). **여러 단어 트리거는 구 전체를 찾는다** — 토큰 하나씩 비교하면 `ice cream`이 문장에 그대로 있어도 매칭되지 않는다(마지막 단어의 규칙 복수만 허용). 토큰 정규식은 앞따옴표를 포함하지 않아 `'inappropriate'` 같은 인용 표기도 잡는다. 표현 문장 번역엔 `[뜻]` 마크업이 없어 어휘의 뜻을 `hint`로 따로 담는다.
  - 어휘 뜻은 `( Phrasal Verb )` 같은 머리글이 첫 줄에 오는 경우가 있어 **`ⓜ` 줄을 우선** 쓴다(`clean_meaning`).
  **[3] 슬롯 치환 단계(`frame`·`baseSegs`·`apply[]`)도 함께 굽는다.** 목적은 슬롯을 맞히는 게 아니라 **유저가 프레임을 직접 산출**하는 것이므로 프레임의 문자 경계를 알아야 한다. **프레임은 `form`(J열)이 아니라 학습 문장(D열)에서 직접 추출한다** — `form`에는 `주어 + didn't + ______`처럼 한글 자리표시자나 `What / Who / Where`처럼 대안이 박힌 표기가 많아 문장에 그대로 등장하지 않는다(J열 기준으로는 661→143만 통과했다). `frame_candidates`가 문장1과 다른 문장의 **공통 구간**을 `difflib.SequenceMatcher`로 뽑는다 — 한 덩어리(`talk to`)뿐 아니라 **떨어진 두 덩어리**(`put` … `on`)도 후보로 낸다(분리형 구동사는 슬롯이 프레임 사이에 들어가므로 연속 구간만 보면 particle이 슬롯으로 새어 `Put your` + 슬롯 `coat on`이 된다). 총 `VE_MIN_FRAME_WORDS`(기본 2)단어 이상이어야 하고, **두 조각 이상일 때만 조각이 모두 기능어인지 검사해 버린다**(흩어진 `you` … `me`가 뽑히면 정작 `Can/Could`가 빠진다 — `FUNCTION_WORDS`). **붙어 있는 한 덩어리는 기능어만이어도 정상 패턴이므로 검사하지 않는다** — 여기에 검사를 걸면 `Was it`·`Did you`·`Do you`가 통째로 빠진다(실제로 54그룹을 잃었다). **조각 끝의 한정사는 떼어내고**(`Put your` → `Put`, 한정사는 뒤따르는 슬롯의 일부다) **한정사만으로 된 조각은 버린다**(`The` … `away`) — 단, 한 덩어리가 한정사로 끝나는 경우(`It's a`)는 그대로 둔다(`trim_determiners`). `extract_frame`이 문장1을 반드시 포함하는 후보 중 **커버 문장 수 → 슬롯이 깨끗한지(`slots_clean`) → 프레임 길이 → 조각 수** 순으로 고른다. 커버를 최우선에 두면 한 문장에만 맞는 과적합 프레임(`Don't be late`)이 걸러지고, `slots_clean`은 슬롯에 모든 문장이 공유하는 단어가 남아 있으면(= 프레임이 미완성) 감점한다. `segment`가 각 문장을 `[{t:'frame'|'slot', s}]`로 분해한다(리터럴을 좌→우로 탐색, 나머지가 슬롯). 추출된 프레임은 `form`보다 **구체적**이다(`주어 + didn't + ___` → `I didn't`, `Do / Does + 주어 + ___ ?` → `Do you`) — [3]에는 구체적인 청크가 유리하다. **추출된 프레임은 `form`과 어휘 내용뿐 아니라 조각 구조까지 일치해야 한다**(`frame_matches_form`·`form_pieces`, 굴절 허용·괄호 안은 선택). 조각 개수와 순서가 맞아야 하므로 `the ___ of the ___`(조각 둘)에서 `of the` 하나만 잡히거나 `What does ___ do ?`에서 끝의 `do`가 빠지면 버린다. 프레임이 form에 없는 낱말을 물고 오거나(`come from` → `It comes from`) 필수 낱말을 빠뜨리는(`What do you think of` → `What` … `you think of`) 경우도 같이 걸린다. 또 **`form`이 문법 틀이면 그룹 자체를 버린다**(`lexical_form`) — 한글 자리표시자나 대안(`Do / Does + 주어 + ___?`)이 있으면 표현이 문장마다 변형돼 `Don't you drive?`와 `Don't they work with you?`가 같은 프레임으로 잡히는 오탐이 생긴다. **홑 대명사 조각도 버린다**(`PRONOUNS`) — `get up`을 배우는데 `You` … `get up`이 프레임이 되면 안 된다. 661그룹 → **222그룹** 통과, 제외 439(문법틀form 198 · 프레임≠form 183 · 공통프레임없음 41 · 버전A불가 12 · 버전B불가 3 · 조인실패 2). **[3]은 정밀도가 재현율보다 중요하다** — 애매한 프레임이 섞이면 "이게 표현이구나"라는 학습 자체가 어긋난다.
- **[5] 상황**(`renderSituation`, `public/vocab-expression.html`) — 기획 의도는 "임의의 상황에서 배운 표현을 가지고 내가 새로 말을 할 수 있다"이고, 학습 방식은 **영어를 보고 영어로 인출**이다. 그래서 푸는 동안 한국어 해석을 노출하지 않고, A 대사 해석(`#aKr`)과 내 답의 뜻은 **답한 뒤에만** 연다.
  **응답은 자유 입력이 아니라 2~3개 중에서 고른다**(듀오링고 `~의 말을 듣고 적절히 답변하세요` 형태). 자유 입력으로 만들었더니 "뭘 써야 할지 감이 안 온다"는 문제가 났는데, 원인은 **답의 범위가 보이지 않는 것**이었다. 선택지는 전부 직전에 배운 문장이고 **오답은 문법은 맞지만 상황에 안 맞는** 문장이라 과제가 **문맥 적합성 판단**이 된다. 오답 후보는 **같은 구간(또는 인접 문항)의 다른 표현 문장**에서 뽑으므로 새 데이터가 필요 없다. 지시문은 한국어지만 generic 하다(`A의 말을 보고 적절히 답변하세요`) — 정답의 번역이 아니다.
  오답을 고르면 그 선택지만 붉게 표시하고 A 해석은 계속 잠긴다. 정답이면 `✓ 좋아요!` + `의미`(내 답의 한국어) + `ⓘ 쓴 표현`(프레임·뜻)을 열고 정답 선택지를 초록으로 표시한다(비교는 `data-ok`로 — 마이크 아이콘이 섞인 `textContent`로 비교하면 빗나간다). **자유 입력 버전도 나란히 둔다** — 능동적 인출 감각은 선택으로는 안 나오기 때문이다. 아무 단서 없이 쓰게 하면 막히고 다 주면 인출이 아니므로 **지원을 한 칸씩만 여는 힌트 사다리**를 쓴다([1] 어휘의 `힌트 보기 → 정답 보기` 규약과 같다): `배운 표현 목록 → 이 상황의 표현 하나 → 첫 단어 → 정답`. 채점은 프레임 포함 여부로만 하므로 모범 답안과 다른 문장(`Hey, you should get up already!`)도 통과한다. 그때 `의미`는 **내가 말한 문장의 뜻**이어야 하므로 모범 답안의 한국어를 거기 붙이지 않고 `ⓘ 이 상황에선 이렇게 말해요`로 따로 뗀다.
  두 방식은 홈 `상황만 풀기` 카드의 **`골라 답하기` / `직접 입력` 두 진입점**으로 각각 들어간다(세션 중 [5]는 `ve:opts.sitType`을 따른다).
  **[5]는 3문항마다 나오므로(`REVIEW_EVERY`) 한참 풀어야 볼 수 있다 — 홈에 `상황만 풀기` 별도 진입점**(`startSituationOnly`)을 둬 상황이 붙은 문항만 연속으로 풀 수 있다. 이 모드에선 `sitOnly=true`이고 `buildStepIndex`가 문항당 1단계로 계산한다. **개발 확인용으로 `devPanel`이 카드 아래 붙어** 이 상황이 어떤 표현으로 만들어졌고 오답이 어디서 왔는지 보여준다(점선·모노 표기로 학습 UI 와 구분, `sitOnly`에서만).
  **A 대사는 소스 CSV에 없어 `scripts/vocab_expression_situations.json`에 손으로 적었다**(학습 문장 `sentence.en`을 키로 하는 `{a_en, a_kr, hint_en}`, 현재 30개. `hint_en`은 자유 입력 버전용 간접 안내로 지금은 화면에 쓰지 않는다). 빌드가 `situation`으로 붙이고, **구간에 상황이 붙은 문항이 없으면 이 단계를 건너뛴다**.
- `public/vocab-expression.html` — **별도** 어휘→표현 프로토타입(자체 완결형 HTML, 본 앱과 무관, `/vocab-expression.html`로 서빙). **앱(말해보카)의 어휘 학습 UI·UX를 화면 그대로 재현**(참고 영상 기반). 흐름은 아이템마다 **[1] 어휘 빈칸 → [2] 표현 문장 배열 → [3]-1 처음 보는 문장에 프레임 넣기(탭) → [3]-2 또 다른 문장에 프레임 타이핑**. **[3]-2 타이핑은 모든 문항에서 항상 거친다** — 응용예문이 하나뿐인 문항은 같은 문장을 다시 쓰지만 조작이 달라 과제가 다르다(있다 없다 하면 흐름이 일정하지 않다). 문항당 4단계이고 `buildStepIndex`가 총 단계 수를 합산해 `setProgress`는 **문항 내 단계 위치**만 받는다. 상단바는 트로피·진행바(별 마커·`i/N`)·설정. 홈은 **버전 A/B 토글**(`ve:ver`) + **진입점 하나** — 들어가면 문항마다 트리거 어휘와 표현이 계속 달라진다.
  **두 버전을 바꿔가며 비교한다** — 어휘 학습 문장을 무엇으로 쓰는지만 다르고 배열 단계·보상은 동일하다:
  - **버전 A** — 어휘는 어휘 예문(`vocabA`)으로 배우고, 표현은 그와 **다른 문장**으로 배운다(현재 앱 구조).
  - **버전 B** — **표현 문장 자체**(`vocabB`)에서 트리거 어휘를 빈칸으로 배우고, 같은 문장을 그대로 배열한다. 어휘 단계에서 정답 문장을 이미 보게 되므로 배열은 '방금 본 문장 다시 만들기'가 되는데, 몸으로 한 번 만들어보는 교재 효과로 **의도한 것**이다.
  - 진행 위치(`ve:pos`)는 **두 버전이 공유**한다 — 버전만 바꿔 같은 문항을 바로 비교할 수 있게. 홈 배치는 `탭 → 시작 카드(흐름·남은 문항 + 큰 CTA `.start-btn`) → 실험 옵션`. 진입 버튼이 다른 카드들과 같은 톤이면 눌러야 들어간다는 게 안 읽히므로 **CTA만 채운 색 버튼**으로 두고 옵션은 `실험 옵션` 헤더 아래 보조 영역에 둔다. `이어서 풀기`/`처음부터 다시 풀기`를 제공하고, 레벨은 문항 카드 좌상단 태그(`레벨 N`, gse 어휘 레벨)로만 표시한다. [1] 어휘는 **채팅 말풍선 카드**(좌상단 `레벨 N` 태그·우상단 `New` 배지·상단 파란→흰 그라데이션): 한국어 번역(정답 뜻 영역 초록 `.tgt`, 괄호 설명줄 muted) 위, 영어 예문의 `[단어]`를 **인라인 입력 빈칸**(`<input class=blank>`, `field-sizing:content`로 자동 폭)으로 보여준다. 하단은 `? 힌트 보기` / `🎤 음성 모드`(비작동). 힌트는 **순차** — `? 힌트 보기`(**앞 2글자**+나머지 길이) → `✔ 정답 보기`(정답 전체, `.blank.revealed`). 둘 다 **placeholder로** 보여주므로 입력값이 남아 있으면 가려져 아무 반응이 없는 것처럼 보인다. 그래서 **정답 보기를 누를 때만 입력값과 오답 박스를 지워** 정답 placeholder가 보이게 하고(힌트 보기는 입력값을 유지), 그 뒤 다시 타이핑해도 정답이 가려지지 않게 **문장 아래에 `정답 {단어}` 주석 한 줄**(`#revealNote`)을 남긴다(정답을 맞히면 숨김). **정답 입력 시 확인 없이 즉시 처리**(`norm`), 오답은 **카드 안 파란 박스**에 `{입력} - 정답과 N글자 달라요`로 틀린 글자 빨강(`diffSpell`). 정답이면 **카드가 초록으로 바뀌고 ✓ 체크** → 정답 단어 초록 박스 + 하단이 **탭(다른 예문·오답 노트·말해보기·북마크·발음 진단) + 3버튼(듣기평가 속도·다시 듣기·다음 문제)**로 교체(다음 문제만 작동). 빈칸 정답은 학습 대상 어휘와 같아야 하므로 다른 단어를 괄호친 예문은 빌드 제외(버전 B는 문장에 쓰인 굴절형이 정답). 버전 B는 표현 문장 번역에 `[뜻]` 마크업이 없으므로 `mark_meaning_kr`가 **뜻 조각을 번역문에서 직접 찾아 `[..]`로 감싼다** — 앱처럼 정답 뜻만 초록으로 보이게(`[옷] 입어.`). **어휘 CSV 예문 번역에 저작된 `[뜻]`을 사전 뜻보다 먼저 시도한다** — 문맥에 맞는 구어체라 더 잘 맞는다(`should`의 사전 뜻 `해야 한다`는 `일어나야 해.`에 없지만 저작된 `야 해`는 있다). 용언은 어미를 떼어 어간으로 맞추고(`공부하다` → `나 [공부] 안 했어`), 한 글자 뜻은 낱말 첫머리에서만 인정한다. 문맥에 맞게 다른 낱말로 번역돼 못 찾는 경우(`bad` → `별로였어?`)가 526문항 중 172개 남으므로 그때만 **`빈칸 뜻 {뜻}` 줄**(`.b-hint`)로 대체한다. [2] 배열은 **말풍선 카드**(한국어 문장 뜻) + **인라인 문장 채우기**(빈 밑줄에 뱅크 단어를 순서대로 탭해 채움, 종결부호 고정) + **단어 뱅크(자리 유지 — 채운 타일은 회색 빈칸으로 남김)** + `정답 보기`/`확인`. 타일은 소문자(첫 단어 노출 방지). 빈 줄 아래 **지시문 한 줄**(`.fill-hint`, `fillHintText`)을 두는데, 데이터에 지시문 필드가 없어 **종결부호로 유도한다**(`?`면 `의문문으로 만들어 주세요`, 그 밖은 `문장을 만들어 주세요`) — 첫 단어를 놓으면 사라진다. 방해 단어는 현재 없음(데이터 준비되면 추가).
  **어휘→표현 연결은 텍스트 설명이 아니라 행위로 체감시킨다**(긴 안내 문구는 한 번에 안 읽히고, 트리거 어휘가 표현의 핵심이 아닐 때 어색해짐):
  - 연결 강화 장치 4종은 **개별 on/off 옵션**(`ve:opts` = `{trigger,banner,preview,fly}`, 기본 `preview`만 ON)으로 조합을 비교할 수 있다. 홈 옵션 박스와 세션 중 상단 기어 팝오버 양쪽에서 토글한다. **기본값은 앱 구현 그대로** — 앱의 단어 뱅크 타일에는 장식이 없으므로 칩 강조·배너·이동 연출은 끈 상태가 기준이고, 켜서 얹었을 때의 효과를 비교한다.
    - `trigger` — **방금 배운 어휘 칩을 초록으로 강조**(`.tile.trigger` + 펄스) + 그 칩을 문장에 넣는 순간 **'써먹었다' 연출**(`fireUsedEffect`: 팝 + 링 반사 + `방금 배운 단어!` 배지 + 햅틱 `navigator.vibrate`, Android만 · 문항당 1회). 끄면 `isTrig` 판정 자체를 건너뛰어 뱅크·완성 문장 모두 장식 없는 기본 타일이 된다.
    - `banner` — 상단바 아래 **단계 배너**(`#stepBanner`, 화면 전환과 무관하게 유지되는 노드): `① 어휘 익히기 → ② 표현 만들기`. 어휘를 맞히면 1단계가 `✓ {단어}` 초록 칩으로 바뀌어 **표현 화면까지 그 단어를 데려간다**. 두 화면이 한 문항의 1·2단계임을 알리는 동시에 단어 캐리오버를 담당. 상태 클래스는 `.sb-step.solved` — 완료 화면의 `.done`(`flex:1; flex-direction:column`)과 겹치면 배너가 깨지므로 `done`을 쓰지 않는다. 옵션 스위치는 `.opt-sw`다 — 짧은 `.sw`를 쓰면 노브(`::after`의 흰 원)가 같은 이름을 쓰던 예문 글자 위에 그려졌다. **짧고 일반적인 클래스명을 새로 만들 때 기존 정의와 겹치는지 먼저 확인할 것**(`.done`·`.sw` 둘 다 이 방식으로 깨졌다).
    - `preview` — 어휘 정답 직후 **다음 단계 예고**: `다음 문제` 버튼 위에 그 버튼을 가리키는 **말풍선**(`.tip-bubble`, `이제 이 단어를 사용해 볼까요?`)을 띄운다. 버튼 문구는 그대로 둔다(단어를 넣어 `{단어} 써보기`로 바꾸면 어색함). 말풍선이 위 탭 라벨을 가리지 않도록 `.vbtns.has-tip`으로 버튼 줄을 내린다.
    - `fly` — 초록 정답 칸에서 다음 화면의 그 단어 자리(배너 칩, 없으면 트리거 타일)로 **단어가 날아가 앉는 이동 연출**(`flyWord`, FLIP 방식 0.5s).
  - 이 장치들이 필요한 이유: 어휘 예문(`Do you have a pen?`)과 표현 문장(`Can I have a pen?`)은 **서로 다른 문장**이고 공유하는 건 단어 하나뿐이라, 화면을 그냥 교체하면 별개 문제 두 개로 읽힌다.
  - **[3] 슬롯 치환(`renderApply(round)`)** — 패턴 학습의 핵심은 슬롯 치환 능력인데 [2]까지는 치환을 한 번도 시키지 않는다. [3]은 **처음 보는 문장**에서 같은 프레임을 다시 쓰게 하며, **한 번은 체감이 덜하므로 2회차까지 간다**: `round 0`은 `apply[0]`을 **뱅크 탭**(프레임을 단어로 쪼개 올려 어순도 과제), `round 1`은 `apply[1]`을 **뱅크 없이 타이핑**(`typing`, 프레임 조각 단위 `<input class=blank>`)으로 승급시킨다. 타이핑 라운드는 [1] 어휘와 같은 규약을 쓴다 — 정답 입력 시 확인 없이 즉시 처리, 오답은 `wrong-box`+`diffSpell`, `힌트 보기`(앞 2글자) → `정답 보기`(입력값 지우고 placeholder + `#revealNote`). 프레임 조각이 둘이면 입력칸도 둘이고 **둘 다 맞아야** 통과한다. `round 0` 정답 후 버튼은 `한 번 더`다. **여기서 유저가 산출해야 하는 건 프레임이다** — 전이되는 자산이 프레임이고 슬롯 단어(`menu`)는 부수적이며 모르는 단어일 수도 있어서, **슬롯은 회색 칩(`.fill-given`)으로 주어진 채 고정하고 프레임 자리만 빈칸으로 비운다**. 그래서 [2]와 조작은 닮았지만 인지 과제가 반대다 — [2]는 프레임을 **숨기고** 어순을 회상시키고, [3]은 프레임을 **비우고** 새 뜻에 얹게 한다. 두 라운드 모두 **슬롯은 회색 칩으로 주어진 채 고정하고 프레임 자리만 비운다** — 1회차는 단어 타일을 탭해서, 2회차는 직접 입력해서 산출한다. 프레임은 단어 단위로 쪼개 올리므로 어순도 과제에 포함되고, 정답 후 원문 대소문자로 교정한다(뱅크 타일은 소문자). **뱅크에는 함정 단어를 섞는다**(`item.traps`) — 정답 조각만 있으면 답이 뻔해 맞혀도 쾌감이 없다.
  - **[3] 결과창(`applyPayoffHtml(item, rows)`)이 효용 체감을 담당한다** — 지금까지 만든 문장을 나란히 놓고 **유저가 매번 직접 놓은 프레임만 초록**으로 묶는다. **기획 의도를 문구로 설명하지 않는다** — `학습 정보` 라벨 + 프레임(초록, 슬롯 표시) + 뜻 + `ⓘ 이 표현으로 만든 문장` + 만든 문장 목록만 둔다. 무엇을 느껴야 하는지 해설하는 문구(`이게 실제로 말이 나오는 순서예요`, `바뀐 건 회색 부분뿐이에요`)는 기획 문서의 언어이고 앱 화면에서는 어색하다. 초록 강조가 같은 틀임을 그대로 보여주므로 해설이 필요 없다. round 0·1의 차이는 목록 줄 수로만 나타난다(`.pf-cmp-en`은 본문을 어둡게 두고 `.tgt`만 초록 — `patternPayoffHtml`의 `.pf-ex-en`은 '전부 초록'이 의도라 클래스를 분리했다). 근거를 코퍼스 수치가 아니라 **자기 산출물**로 두는 것이 요점이다.
  - 배열 화면은 **한국어 뜻 + 빈 줄 + 지시문**만 둔다 — 앱 화면에 없는 안내 박스(`이번에 배울 표현` 보라 박스)는 두지 않는다. 상황 태그(일상/여행/비즈니스)와 활용 규모 수치(`N가지`)도 **쓰지 않는다** — 태그는 콘텐츠에서 없어질 수 있고, 수치는 실제 용례가 더 많을 수 있어 의미가 없다. **영어 프레임(`pattern.form`)은 정답 어순이 새므로 배열 전엔 절대 노출하지 않는다** — 정답 후 `학습 정보`에서 처음 공개한다.
  - `trigger` 칩 강조가 켜졌을 때 어느 타일이 그 어휘인지는 `isTriggerToken`이 판정 — 정확일치 + 규칙 굴절형(days/going/taxes/fatter/dancing/parties, 소유격 `toilet's`, 복합어 `Round-trip⊃trip`)까지 인정하고 **하이픈 조각 단위로 비교**해 `wondering⊃ring` 같은 오탐을 막는다. 칩 위 배지는 별도 옵션(`ve:chipLabel`: `none` 라벨 없음(기본) / `learned`=`방금 배운` / `meaning`=그 어휘의 뜻)으로, 홈의 세그먼트 컨트롤과 세션 중 상단 기어(설정 팝오버) 양쪽에서 바꿀 수 있고 세션 중 전환 시 즉시 반영된다(`applyChipLabels`, CSS `content:attr(data-label)` — 라벨이 없으면 `data-label`을 지워 빈 배지가 그려지지 않게 한다). 뜻 라벨은 `shortMeaning`이 `<주석>`·`(부연)`을 떼고 앞쪽 뜻만 10자 내로 줄인다.
  - 정답 시 **초록 카드 + ✓**, 완성 문장은 **타일 그대로 두고**(원문 대소문자로 교정 — 텍스트로 바꾸면 방금 만든 결과물이라는 감각이 끊긴다) 그 아래 **`학습 정보` 섹션**(`patternPayoffHtml`)을 붙인다: `💡 새로운 표현을 배웠어요` + **재사용 프레임 공개**(`pattern.form`의 `______`/`~`를 빈칸으로 그린 초록 큰 글씨 + `pattern.meaning` 회색 뜻) + `pattern.desc`가 있으면 회색 박스 한 줄 + `ⓘ 단어만 바꿔서 이렇게도 말할 수 있어요` + **응용 예문 2개**(`siblings`, 구분 없이 전부 초록 — 내부자 테스트 기준). 배열 중엔 숨겨둔 영어 틀을 정답 후에 처음 공개하는 구성이라 여기에 새 정보가 담긴다. 하단도 어휘 단계처럼 **탭(사전 검색·오답 노트·말해보기·북마크·발음 진단) + 3버튼(표현 사전·다시 듣기·다음 문제)**으로 바뀐다. `학습 정보`는 카드 폭을 꽉 채운 흰 블록(`.payoff`의 음수 마진)이다 — 카드의 두 톤 배경이 **높이 비율로** 갈리므로 그냥 두면 분기점이 섹션 중간을 지나간다.
  - 데이터 스키마: `items[]` = `{trigger,word,meaning,pos,cefr,level,rank, vocabA{answer,enLines,koLines}, vocabB{answer,enLines,koLines,hint}, sentence{en,kr,trigger}, pattern{form,meaning,desc}, frame[리터럴 조각], traps[함정 단어], baseSegs[{t,s}], apply[{en,kr,punct,segs[{t,s}]}], siblings[{en,kr}]}`. 함정은 `make_traps`가 만든다 — 프레임의 기능어 자리에 **같은 부류**(particle/prep/aux/pron)의 다른 낱말을 붙이고(엉뚱한 품사를 섞으면 오히려 정답이 뻔해진다), 기능어가 없는 프레임은 **다른 표현의 프레임 낱말**을 길이가 비슷한 것에서 빌린다. 문장에 이미 쓰인 낱말은 제외하고, 난수 없이 문장 해시로 골라 빌드가 재현된다(개수 `VE_TRAPS`, 기본 2). TTS·음성 등 앱 부가기능은 시각적으로만 재현(비작동), 리딩·타이핑 중심.
- `public/listening-pingpong.html` — **별도** 리스닝 핑퐁 프로토타입(자체 완결형 HTML + 브라우저 TTS, 본 앱과 무관, `/listening-pingpong.html`로 서빙). 콘텐츠는 외부 JSON에서 로드: `listening-pingpong.data.json`(초급 A1-A2) + `listening-pingpong.data.b.json`(중급 B1-B2). 홈은 **초급/중급 그룹**으로 나눠 보여주고, 완료한 대화는 `localStorage`(`lp:cleared`)에 저장해 ✓ 완료 배지·`n/N 완료` 카운트로 표시(하나씩 클리어). 흐름은 **표현(단어 배열) → 리스닝**: A(상대)가 트리거 문장을 말하고 B(학습자)가 3지선다로 답하는 오청(mishearing) 함정 퀴즈. A 대사는 텍스트 없이 음성만(시작 전 3·2·1 카운트다운), 답하면 각 선택지의 한국어 뜻·해설을 보여준다. **답한 뒤**에는 그 A 말풍선을 탭해 영어 대사를 펼쳐 볼 수 있다(답하기 전엔 잠금 — 훈련 훼손 방지, `setupPeek`/`unlockA`). 홈의 **'A 노출' 옵션**(`lp:expose`)으로 A 대사 방식을 고른다: `듣기`(음성) 또는 `고정·읽기`(음성 없이 단어를 하나씩 제자리 교체하는 RSVP, `rsvpReveal`) — 리딩은 **대화 영역(chat) 위에만 겹치는 중앙 오버레이**로, 뒤 대화를 흐리게(blur) 덮고 상단 진행바는 유지하며 부드럽게 페이드 인/아웃한다(화면 통째 교체 금지 — 되돌아올 때 끊김 방지, `chat.getBoundingClientRect()`로 위치·크기 지정). 고정 모드에선 **속도 설정이 RSVP 속도를 겸하고**(음절수×280/rate), 준비 문구·다시 보기가 모두 '보기'로 바뀐다. **틀리면** A 대사를 다시 접하고 그 문장을 단어 배열로 재구성하는 교정 단계가 뜬다(`buildArrange`) — 듣기는 오답 뒤 음성을 자동 재생하지만, **고정은 갑자기 뜨지 않도록 '👀 다시 보기'를 눌러야** 리딩이 노출된다(`makeRsvpPlayer`, 자동 재생 없음). 표현이 나온 트리거 문제에서는 정답 후 "🎧 방금 A가 이렇게 말했어요 + 그 문장 + 배운 표현" 박스로 연결해준다. done 화면에서 전체 스크립트(영어+한국어)를 볼 수 있다. 데이터 스키마: `examples[]`에 `vocab`·`expression{csv_id,en,kr}`·`dialogue[{role,text,kr}|{role:'B',choices:[{text,kr,correct,trap,explain,highlight}]}]`; 표현은 실제 표현 뱅크(ExpressionUnitKR) 항목(csv_id=seq), '표현 배열' 문장은 트리거 A 대사(표현에 `~`가 있으면)에서 자동 생성. 강조(형광펜/밑줄)는 현재 쓰지 않음. A/B는 서로 다른 음성(여성/남성 우선)으로 재생하며 홈에서 각각 고른다.
- `public/listening-pingpong-v2.html` — 리스닝 핑퐁 **UI v2**(`/listening-pingpong-v2.html`). 데이터 JSON과 `lp:*` localStorage는 v1과 공유하되 UI를 재구성: **표현(단어 배열) 선행 단계 제거**(홈 → 바로 리스닝), 화면은 **상단 문제 카드(레벨 헤더 + 대화) + 하단 답안 영역** 2단. 문제 전환은 **스왑 체인(크기 2)** — 결과를 보여주는 동안 `back` 버퍼(`visibility:hidden`이라 레이아웃·스크롤 유지)에 다음 문제 화면을 미리 렌더해 두고 '다음'에서 교체(`swapScreens`, 슬라이드 인). 각 문제 화면은 매번 새로 만드는 별개 컴포넌트고, 이전 대화 내역은 DOM 복제가 아니라 **turns 데이터에서 재렌더**(`newChatScreen`) — 위로 스크롤해 열람하되 손을 떼면 현재 문답으로 복귀(sticky, `attachSnapBack`). 답변 흐름: 답하면 B 정답 대사가 바로 말풍선으로 보이고 💡 해설(정답 초록/오답 노랑, 오답이면 고른 문장 + 수평선)이 카드에 남는다. **정답 → A 말풍선 탭 열람 허용 후 다음 문답 / 오답 → A 대사 계속 숨긴 채 문장 배열 교정 문제**(카드에 뜻·플레이어·배열 줄, 답안 영역에 단어 뱅크). 결과 영역의 **오답 노트** 버튼은 모범답안 + 오답 이유 팝업(`showWrongNote`). A 말풍선은 재생 전/후에도 정적 웨이브 유지(크기 고정).
- `public/listening-pingpong-v3.html` — 리스닝 핑퐁 **UI v3**(`/listening-pingpong-v3.html`). 데이터 JSON·`lp:*` localStorage는 v1/v2와 공유. v2의 문제 카드 안에서 **대화가 v1처럼 메신저식으로 아래로 누적**된다(스왑 체인·재렌더 없음). 채팅은 일반 column 스크롤러 + **JS 하단 고정**(`pinBottom` — 패널이 오르내리는 동안 rAF로 매 프레임 바닥 고정; column-reverse 네이티브 고정은 iOS Safari의 시작 방향 overflow 접근 불가 WebKit 버그로 깨져서 쓰지 않는다) — 그래서 **선택지 패널이 카드 하단에서 키보드처럼 올라올 때 대화가 위로 밀리고, 내려갈 때 함께 내려온다**(패널은 `grid-template-rows 0fr→1fr` 애니메이션, `raisePanel`/`lowerPanel`). 답하면 각 선택지에 뜻·해설이 **인라인** 표시(v1 방식, 별도 오답 노트 팝업 없음)되고 B 정답 말풍선이 채팅에 쌓이며, **'다음'을 눌러야 패널이 내려간다**. **오답 교정(문장 배열)은 화면 교체 대신 바텀 시트**(`fixSheet` — 카드 톤의 안내·뜻·플레이어·배열 줄 + 아래 단어 뱅크)가 올라와 풀고 나면 내려간다(뒤의 대화 유지). A 말풍선 열람은 정답 시 즉시, 오답 시 배열 완료 후 허용. **다시 듣기는 A 말풍선 안 스피커 버튼**(구분선 옆 본문은 웨이브 → '영어 보기' → 영어 전문으로 바뀜, 각 말풍선이 자기 대사를 재생) — 카드 하단 버튼 없음.
- `public/listening-pingpong-r1.html` — v3의 레이아웃 변형(`/listening-pingpong-r1.html`). 선택지 패널을 **카드 밖(아래)으로 분리**하고 `min-height:40vh`로 하단 몫을 상시 예약해 **카드 최대 크기를 v2 수준으로 고정**(기본 선택지 3개는 예약분 안이라 첫 등장 시 카드 불변). 패널은 **바텀시트처럼 아래에서 슬라이드 인/아웃**(transform, 내려간 상태는 overflow:hidden으로 클립, 내용은 박스 하단에 붙음). 해설 등으로 내용이 예약분을 넘으면 카드가 그만큼 줄고, '다음'으로 내려간 뒤 내용을 비울 때는 **height 트랜지션으로 원래 크기 복귀**(툭 튀는 것 방지). 답변 직후 A 문장은 기본으로 펼쳐짐(탭으로 숨김 토글). 그 외 흐름·데이터는 v3와 동일.

화면 흐름: 스토리 선택 → 에피소드(토픽) 목록 → 시작 화면(모드 선택) → 세션(리딩/리스닝 재생) → 퀴즈(바텀시트). `App.tsx`가 상태기계로 관리(`mode`·`playing` 상태).

## 데이터 파이프라인

`dialogs.json`은 커밋된 생성물이자 런타임의 유일한 소스다(sqlite 없이도 앱은 동작). 재생성할 때만 `scripts/extract.py`를 쓰고, **손으로 편집하지 말 것**.

- 출처: bi의 `server.sqlite` (이 레포 밖). 경로는 `READING_POC_SQLITE` 환경변수 또는 CLI 인자로 지정. 없으면 명확한 에러로 종료한다.
  - 필수 테이블: `ConversationDialogScriptKR`(스크립트 + `voiceInfos` 해시), `ResourceFileKR`의 `ConversationCourses`(코스/토픽 + 대화별 `di[].h` = dialogHash). 사전 전용 오래된 export에는 대화 테이블이 없으니 **최신 export를 쓸 것**.
- 포함 코스: `INCLUDE = [2(뉴욕여행·추천), 25(잡스), 17(투자)]`(배열 순서 = 화면 표시 순서), 레벨 A2(=2) 고정. 첫 코스(2)는 `StoryList`에서 "추천" 배지.
- 각 에피소드는 **도입부 4턴(`MAX_SCRIPTS`)만** 사용 — 리딩 분량을 짧게.
- 단어 경계는 tagList가 아니라 문장 문자열을 직접 파싱(문장부호 보존).
- **리스닝 오디오 URL**을 스크립트마다 `audioUrl`로 구워 넣는다. 형식:
  `https://speech.epop.ai/conversation/tts/{dialogSeq:06d}-{dialogHash}/{voiceType}/{scriptIdx:02d}.{voiceHash}.ogg`
  (dialogSeq = topicSeq×10+level, scriptIdx = scriptSeq%100, voiceType = `voiceInfos`의 non-null 인덱스 폴더명, voiceHash = 그 해시). CDN은 인증 없는 공개 접근이라 런타임에서 바로 재생된다.

`quizzes.json`은 `topicSeq`(코스별로 유니크)를 키로 하는 수기 저작 데이터. 대화당 1문항.

## Conventions

- 주석·UI 문구는 한국어. 식별자만 영어.
- 순수 로직(`chunk.ts`, `reveal.ts`, hook)은 TDD. `src/**/*.test.ts(x)`.
- 생성물 fixture(`dialogs.json`)도 커밋한다(런타임 sqlite 의존 0).
- 음절 계산은 `syllable` 패키지.

## Quirks / 주의

- **노출 규칙**: 청크 i는 (i+N)번째 청크가 등장하는 순간 사라진다(항상 최근 N개 유지). 문장 끝의 마지막 N개는 등장할 다음 청크가 없으므로, 평균 dwell 간격으로 하나씩 빠지게 스케줄을 이어붙인다(그러지 않으면 마지막 창이 동시에 사라져 끝 청크를 볼 시간이 부족 — `buildRevealSchedule`의 `showAtExt`, 간격 상한 `DRAIN_STEP_MS`). dwell = `max(100ms, 음절수 × 속도설정)`(하한이 높으면 짧은 단어가 바닥에 걸려 속도 설정이 안 먹히므로 `MIN_DWELL_MS`는 낮게 유지). 기본은 opacity 페이드(등장 450ms, 사라짐은 250ms로 더 빠르게, 자리는 유지)이며 등장/사라짐을 각각 "페이드 인"(`fadeIn`)·"페이드 아웃"(`fadeOut`)으로 끌 수 있다. transition은 `DialogBubble`이 방향별로 인라인 지정한다(값이 바뀌는 순간의 transition이 등장/사라짐을 결정 — 보이면 fadeIn, 숨기면 fadeOut). "오래된 청크 숨기기"를 끄면(`hideOld=false`) 창 크기를 무시하고 문장 끝까지 누적한다. 상세는 `reveal.ts`.
- **퀴즈 정답은 반드시 노출되는 턴(현재 4턴) 안에 있어야 한다.** `MAX_SCRIPTS`를 줄이면 정답이 잘린 문제가 생기므로 해당 퀴즈를 재작성해야 한다. 재생성 후 `dialogs.json`에 정답 근거 문구가 있는지 확인할 것.
- **localStorage 키**: `reading-poc:settings`(설정) · `reading-poc:solved`(푼 토픽). 기존 저장값이 있으면 기본값보다 우선하므로, 새 기본값 확인 시 저장소를 비운다.
- **모드 선택·다시 듣기**: 토픽 진입 시 `SessionStart`(모드 선택 + 리딩 설정)를 거쳐 재생. 리딩 노출 설정은 리딩 모드에서만 노출. 퀴즈의 "다시 듣기"는 시작 화면을 건너뛰고 **같은 모드로 즉시** 처음부터 재생(`sessionRun` 증가로 세션 리마운트).
- **리스닝 재생**: 문장 단위 오디오를 순차 재생, `ended`(또는 `error`)에 다음으로. 자동재생은 시작 버튼 클릭(사용자 제스처) 직후라 허용된다. 텍스트는 숨기고 재생 중 말풍선에만 웨이브 인디케이터를 표시.
- **고정 재생**: `FixedSession`이 화면 중앙 한 곳에서 단어/청크를 제자리 교체(RSVP). 창·페이드·hideOld 등은 무관하고 **노출 단위(단어/청크)와 속도(ms/음절)만** 따른다(dwell = `max(100ms, 음절수×속도)`, `reveal.ts`의 `dwellMs` 재사용). 대화 맥락은 현재 화자(A/B)·진행바로 유지. 시작 화면에서 고정 모드는 단위·속도만 조절.
- **전광판 재생**: 시작 화면에서 하위 방식(`marqueeStyle`)을 고른다. `MarqueeSession`은 진입점으로 방식에 따라 분기.
  - **문장별**(`sentence`, 기본): 각 말풍선(`MarqueeSentenceSession`)이 자기 텍스트를 Web Animations API로 오른쪽→왼쪽 1회 흘려보낸다(`overflow:hidden` 레인, 흐름 뒤엔 빈 말풍선). 다음 말풍선은 현재 문장이 화면에 다 들어온 시점에 **겹쳐서** 등장해 풀-스톱 없이 연속 흐른다.
  - **한 줄**(`stream`, `MarqueeStreamSession`): 고정된 한 줄 레인에서 문장 전체가 오른쪽→왼쪽으로 연속으로 흐른다(여러 단어가 이어서 보임). 화자(A/B)·진행바로 맥락 유지, 문장 단위로 순차 진행.
  - 속도는 공통 `marqueeSpeed`(px/초, 40~400).
- **Vercel 배포**: 커밋 author 이메일이 GitHub 계정(namda-on)에 등록된 주소여야 배포가 식별된다. 이 레포는 `user.email = namda1571@gmail.com`으로 설정돼 있다.
- **화자 표시는 A/B**, 에피소드 제목은 "Episode N"만 — 제목·상대역이 주제를 노출해 문제가 쉬워지는 것을 막기 위함.
- **리딩 시작 준비 신호**: 곧바로 노출되면 당황스러우므로, 첫 텍스트 자리에 dot 을 `READY_MS`(0.8s) 동안 깜빡인 뒤 재생을 시작한다(`ready` 게이트).
- **리딩 옵션 프리셋**: `SettingsPanel`은 프리셋 3개(단어 / 청크 / 누적)를 원클릭 제공하고, "직접 설정"을 펼치면 개별 옵션을 조절한다(옵션 설명은 그 안 맨 아래에 표시). 프리셋은 노출 관련 필드(단위·창·속도·hideOld·최대청크)와 **페이드 인/아웃**을 지정한다(청크 프리셋은 창2 — 다음 청크 등장 시 이전 청크가 밀려나는 슬라이딩이 보이도록, 청크·누적은 페이드 인 켜기가 기본). **끝 사라짐 간격·전광판 속도는 프리셋과 독립**(프리셋 클릭 시 유지, `matchesPreset` 판정에서도 제외). 현재 설정이 프리셋과 일치하면 하이라이트, 아니면 "커스텀" 배지.
- **설정 지속**: 설정은 `reading-poc:settings`에 저장돼 마지막 상태가 유지된다(진입 시 리셋하지 않음). 마지막에 고른 모드(`reading-poc:mode`)와 문제 유형(`reading-poc:quizType`)도 저장돼 다음 토픽 진입 시 기본 선택된다(`App`이 저장, `SessionStart`의 `initialMode`·`initialQuizType`). 기본값(`DEFAULT_SETTINGS`)은 단어·창4·속도300·hideOld켜기·**페이드 인 끄기/아웃 켜기**.
- **문제 유형**: 세션 후 문제를 시작 화면에서 고른다 — `comprehension`(객관식 이해, `quizzes.json`), `arrange`(한국어 뜻→영어 단어 순서 맞추기), `dictation`(오디오 듣고 타이핑, 대소문자·문장부호 무시 채점). 배열·받아쓰기는 `pickKeySentence`로 고른 **단일** 핵심 문장(퀴즈 정답 근거 문장)을 대상으로 하며 기존 데이터(english·translated·audioUrl)만 사용. 마지막 유형은 `reading-poc:quizType`에 저장. 다시 풀 때 유형을 바꿔 지루함을 줄인다.
- **문제 미리보기**: 이해(`comprehension`) 유형일 때만 세션 상단에 `QuestionBanner`로 질문을 처음부터 노출한다(`App`이 `showQuestion`으로 전달; 배열·받아쓰기는 어떤 문장이 나올지가 문제라 숨김). 질문은 `quizzes.json`에서 `topicSeq`로 읽고, 선택지는 끝난 뒤 `QuizSheet`에서.
- **퀴즈 보기 셔플**: 저작 데이터(`quizzes.json`)가 정답을 대부분 1번(`answerIndex:0`)에 두어 위치로 답을 유추할 수 있으므로, `Quiz`가 마운트 시 보기를 1회 셔플한다(정답 위치 균등 분산). 데이터의 `answerIndex`는 그대로 두고 런타임에서만 섞는다.
