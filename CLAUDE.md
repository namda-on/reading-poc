# CLAUDE.md

## Project overview

리스닝이 불가능한 환경에서도 같은 회화 콘텐츠로 훈련하는 **리딩 모드 POC**.
리스닝의 "실시간 처리"를 소리 없이 재현한다 — 문장 크기만큼 확보된 말풍선 위를 N개짜리 창(window)이 청크의 실제 위치를 따라 슬라이딩하며 노출하고, 지나간/미래 청크는 빈 공백으로 남겨 되돌아가기·미리보기를 차단한다. 대화가 끝나면 한국어 객관식 1문항을 바텀시트로 푼다.

세션 시작 화면에서 **리딩 / 리스닝 / 전광판 / 고정** 모드를 고른다. 리스닝은 같은 대화를 실제 TTS 음성(speech.epop.ai)으로 재생하되 텍스트는 완전히 숨긴다(순수 듣기). 전광판은 같은 말풍선 레이아웃에서 각 문장이 오른쪽→왼쪽으로 흘러 지나가는 LED 전광판식 노출이다. 고정은 시선을 한 곳에 둔 채 화면 중앙에서 단어/청크가 제자리 교체되는 RSVP식 노출이다(시선 이동 없이 읽기 비교용). 모든 모드가 끝나면 같은 퀴즈로 이어진다.

**주관적 체험용 프로토타입**이다. 정량 로깅·서버·백엔드 없음. 실험 축은 노출 단위(단어/청크)·창 크기 N·속도(ms/음절).

설계·구현 계획 문서: `docs/superpowers/specs/`, `docs/superpowers/plans/`.

## Commands

```bash
npm run dev            # 개발 서버
npm run build          # tsc -b && vite build
npm test               # vitest (chunk / reveal / useSlidingReveal)
python3 scripts/extract.py   # 대화 데이터(dialogs.json) 재생성
```

## Directory map

- `src/lib/chunk.ts` — 문장 → 청크 분할(순수 함수). 단어 모드 / 규칙 모드.
- `src/lib/reveal.ts` — 노출 스케줄(순수 함수). 음절 기반 dwell 계산.
- `src/hooks/useSlidingReveal.ts` — 스케줄을 타이머로 구동(generation guard).
- `src/components/` — `StoryList`(첫 화면) · `TopicList` · `SessionStart`(모드 선택 시작 화면) · `ReadingSession`(리딩 재생) · `ListeningSession`(리스닝 오디오 재생, 텍스트 숨김) · `MarqueeSession`(전광판식 가로 흐름) · `FixedSession`(시선 고정 RSVP) · `DialogBubble` · `SettingsPanel` · `QuizSheet`(문제 바텀시트) · `Quiz`(객관식)/`ArrangeQuiz`(단어 배열)/`DictationQuiz`(받아쓰기).
- `src/settings/SettingsContext.tsx` — 설정 상태 + localStorage.
- `src/lib/progress.ts` — 푼 토픽 localStorage 저장.
- `src/lib/keySentence.ts` — 배열·받아쓰기 문제에 쓸 '핵심 1문장' 선택. 스크립트를 문장 단위로 쪼개(항상 단일 문장), 퀴즈 힌트(질문·정답·해설)와 한국어 글자 바이그램 겹침이 가장 큰 문장을 고른다(정답 근거 문장). 힌트/겹침이 없으면 3~9단어 중 가장 긴 문장.
- `src/lib/quizHint.ts` — `topicSeq`의 퀴즈 질문·정답·해설을 합친 한국어 힌트(핵심 문장 선정용).
- `src/data/` — `dialogs.json`(생성물, 커밋됨) · `quizzes.json`(수기 저작) · `types.ts` · `quizTypes.ts`.
- `scripts/extract.py` — 외부 sqlite에서 대화 추출.
- `scripts/build_vocab_expression.py` — **어휘→표현** 프로토타입 데이터 생성(레포 밖 CSV → `public/vocab-expression.data.json`). 소스는 `VE_EXPR_CSV`(기본 `유기적 통합모드 문장 (공유용) - 문장1 변형후보 추가 (검토중).csv`)·`VE_VOCAB_CSV`(`All_*.csv`)·`VE_GSE_CSV`(`gse_corrected_final_0624 - 보정결과_전체.csv`) 환경변수. 표현 CSV는 **그룹(id)당** 트리거 어휘(`trigger`,`trigger_dictseq`) + 표현 패턴(`표제어 또는 회화패턴`) + 문장 3~10개(`문장1`=트리거가 들어간 대표 문장, `문장1 변형후보`들이 뒤에 붙는다) + 결과 카드(`카드 초록(형태)`/`회색1행(뜻)`/`회색2행(설명)`) 구조이며, 그룹 첫 행에만 값이 있어 **forward-fill**로 읽는다. **문장 열은 위치가 아니라 이름으로 찾는다**(`EXPR_COLS` 별칭 맵) — 소스에 열이 추가되면 위치 기반 파싱은 조용히 다른 열을 읽는다. 별칭에 없는 이름이 오면 `die()`로 멈춘다(조용히 문장을 흘리지 않는다). `jl 검토` 열의 작업 메모(`트리거 재작업` 9 · `번역 수정 필요` 5 등 17그룹)는 **게이팅에 쓰지 않는다** — 콘텐츠 측 진행 상태이고 프레임 추출 가능 여부와 무관하다. `trigger_dictseq`로 어휘 CSV(`seq`)와 gse(`dictSeq`)에 조인해 어휘 예문·뜻·품사·CEFR·레벨을 붙인다. 정렬은 `trigger_newRank3`(빈도) 오름차순, 문항 수는 `VE_MAX_ITEMS`(**기본 0 = 전체**, 조건 통과 261그룹 전부를 굽는다. 세션을 짧게 줄이려면 숫자를 준다).
  **한 문항에 두 버전의 어휘 문장을 함께 굽는다**(같은 문항을 두 방식으로 비교해야 하므로 양쪽 모두 성립하는 그룹만 사용):
  - `vocabA` — 어휘 CSV의 `learnSentence`(표현 문장과 **다른** 문장). 막아야 하는 건 **여러 단어 표제어를 일부만 괄호친 예문**(`baggage claim`을 배우는데 빈칸이 `[baggage]`)이고, 단일 단어 표제어의 빈칸은 굴절형이어도(`child`→`[children]`) 인정한다. 복합어가 빈칸 두 개로 쪼개진 경우(`[ice] [cream]`)는 `merge_blanks`가 하나로 잇는데, **합친 결과가 학습 어휘를 가리킬 때만** 합친다 — `[went] [to]`는 `went to`가 되어 어휘(`go`)와 무관하므로 합치지 않고 그대로 제외한다(UI가 첫 빈칸만 입력칸으로 만들어 나머지가 대괄호로 남으므로 빈칸이 둘 이상이면 버린다).
  - `vocabB` — **표현 문장(문장1)에서 트리거 어휘를 빈칸으로** 만든 것(`blank_trigger`). 굴절형도 인정하므로 정답은 문장에 쓰인 표면형(`parent`→`parents`). **여러 단어 트리거는 구 전체를 찾는다** — 토큰 하나씩 비교하면 `ice cream`이 문장에 그대로 있어도 매칭되지 않는다(마지막 단어의 규칙 복수만 허용). 토큰 정규식은 앞따옴표를 포함하지 않아 `'inappropriate'` 같은 인용 표기도 잡는다. 표현 문장 번역엔 `[뜻]` 마크업이 없어 어휘의 뜻을 `hint`로 따로 담는다.
  - 어휘 뜻은 `( Phrasal Verb )` 같은 머리글이 첫 줄에 오는 경우가 있어 **`ⓜ` 줄을 우선** 쓴다(`clean_meaning`).
  **표현 산출 단계(`frame`·`baseSegs`·`apply[]`)도 함께 굽는다.** 목적은 슬롯을 맞히는 게 아니라 **유저가 프레임을 직접 산출**하는 것이므로 프레임의 문자 경계를 알아야 한다. **프레임은 `form`(J열)이 아니라 학습 문장(D열)에서 직접 추출한다** — `form`에는 `주어 + didn't + ______`처럼 한글 자리표시자나 `What / Who / Where`처럼 대안이 박힌 표기가 많아 문장에 그대로 등장하지 않는다(J열 기준으로는 661→143만 통과했다). `frame_candidates`가 문장1과 다른 문장의 **공통 구간**을 `difflib.SequenceMatcher`로 뽑는다 — 한 덩어리(`talk to`)뿐 아니라 **떨어진 두 덩어리**(`put` … `on`)도 후보로 낸다(분리형 구동사는 슬롯이 프레임 사이에 들어가므로 연속 구간만 보면 particle이 슬롯으로 새어 `Put your` + 슬롯 `coat on`이 된다). 총 `VE_MIN_FRAME_WORDS`(기본 2)단어 이상이어야 하고, **두 조각 이상일 때만 조각이 모두 기능어인지 검사해 버린다**(흩어진 `you` … `me`가 뽑히면 정작 `Can/Could`가 빠진다 — `FUNCTION_WORDS`). **붙어 있는 한 덩어리는 기능어만이어도 정상 패턴이므로 검사하지 않는다** — 여기에 검사를 걸면 `Was it`·`Did you`·`Do you`가 통째로 빠진다(실제로 54그룹을 잃었다). **조각 끝의 한정사는 떼어내고**(`Put your` → `Put`, 한정사는 뒤따르는 슬롯의 일부다) **한정사만으로 된 조각은 버린다**(`The` … `away`) — 단, 한 덩어리가 한정사로 끝나는 경우(`It's a`)는 그대로 둔다(`trim_determiners`). `extract_frame`이 문장1을 반드시 포함하는 후보 중 **`form`과 일치하는지(`frame_matches_form`) → 커버 문장 수 → 슬롯이 깨끗한지(`slots_clean`) → 프레임 길이 → 조각 수** 순으로 고른다. **form 일치가 첫 키여야 한다** — 커버를 먼저 보면 변형 후보가 많은 그룹에서 여러 문장을 덮는 과적합 프레임이 이겨 버리고, 그것이 사후 form 검사에서 탈락해 그룹 자체를 잃는다(같은 소스로 213 → 261). 커버는 그 안에서 한 문장에만 맞는 프레임(`Don't be late`)을 걸러내고, `slots_clean`은 슬롯에 모든 문장이 공유하는 단어가 남아 있으면(= 프레임이 미완성) 감점한다. **`expr_meaning_spans`의 후보 순서는 결정적이어야 한다** — `set`을 길이만으로 정렬하면 같은 길이 후보의 순서가 `PYTHONHASHSEED`에 좌우돼 **같은 입력에서 빌드마다 다른 구간이 칠해진다**(`[내가] 할게` / `내가 [할게]`). 그래서 후보마다 어느 낱말에서 나왔는지를 같이 들고 `긴 것 → 뒤쪽 낱말 → 앞쪽 글자 → 사전순`으로 못 박는다. 뒤쪽 낱말을 앞세우는 이유는 표현의 머리가 용언이기 때문이다(`~에게 다시 전화하다`에서 칠할 것은 `다시`가 아니라 `전화`다).
  **표현 구간 마크업(`krMark`)은 학습 문장뿐 아니라 `apply[]`·`siblings[]`에도 굽는다** — `[4]`가 형제 문장을 대상으로 쓰므로 그쪽 번역에도 같은 신호가 필요하다(`mark_expr_kr`, 학습 문장 207/261 · `apply` 1098/1441 · `siblings` 402/522). `segment`가 각 문장을 `[{t:'frame'|'slot', s}]`로 분해한다(리터럴을 좌→우로 탐색, 나머지가 슬롯). 추출된 프레임은 `form`보다 **구체적**이다(`주어 + didn't + ___` → `I didn't`, `Do / Does + 주어 + ___ ?` → `Do you`) — [3]에는 구체적인 청크가 유리하다. **추출된 프레임은 `form`과 어휘 내용뿐 아니라 조각 구조까지 일치해야 한다**(`frame_matches_form`·`form_pieces`, 굴절 허용·괄호 안은 선택). 조각 개수와 순서가 맞아야 하므로 `the ___ of the ___`(조각 둘)에서 `of the` 하나만 잡히거나 `What does ___ do ?`에서 끝의 `do`가 빠지면 버린다. 프레임이 form에 없는 낱말을 물고 오거나(`come from` → `It comes from`) 필수 낱말을 빠뜨리는(`What do you think of` → `What` … `you think of`) 경우도 같이 걸린다. 또 **`form`이 문법 틀이면 그룹 자체를 버린다**(`lexical_form`) — 한글 자리표시자나 대안(`Do / Does + 주어 + ___?`)이 있으면 표현이 문장마다 변형돼 `Don't you drive?`와 `Don't they work with you?`가 같은 프레임으로 잡히는 오탐이 생긴다. **홑 대명사 조각도 버린다**(`PRONOUNS`) — `get up`을 배우는데 `You` … `get up`이 프레임이 되면 안 된다. 661그룹 → **261그룹** 통과, 제외 400(문법틀form 198 · 프레임≠form 182 · 공통프레임없음 2 · 버전A불가 12 · 버전B불가 4 · 조인실패 2). **[3]은 정밀도가 재현율보다 중요하다** — 애매한 프레임이 섞이면 "이게 표현이구나"라는 학습 자체가 어긋난다.
- **[5] 상황**(`renderSituation`, `public/vocab-expression.html`) — 기획 의도는 "임의의 상황에서 배운 표현을 가지고 내가 새로 말을 할 수 있다"이고, 학습 방식은 **영어를 보고 영어로 인출**이다. 그래서 푸는 동안 한국어 해석을 노출하지 않고, A 대사 해석(`#aKr`)과 내 답의 뜻은 **답한 뒤에만** 연다.
  **응답은 자유 입력이 아니라 고르는데, 고르는 대상은 문장이 아니라 표현이고 그 뒤 문장을 직접 만든다**. 문장 3개를 늘어놓으면 과제가 **A의 영어를 해석하고 긴 문장 셋을 비교하는 일**이 되어 정작 "배운 표현을 여기서 말한다"는 감각이 나오지 않는다(자유 입력은 반대로 "뭘 써야 할지 감이 안 온다" — 답의 범위가 안 보인다). 그래서 두 박자로 쪼갠다:
  ① **표현 고르기**(`renderPick`) — 선택지를 프레임 칩(`put ~ on`·`get up`, `.sit-opt.fr`)으로 줄인다. 읽는 부담이 크게 줄고 고르는 대상이 내가 배운 표현이 된다. 오답 후보는 **같은 구간(또는 인접 문항)의 다른 표현**에서 뽑고 프레임이 겹치면 걸러낸다(칩이 같아 보이면 안 된다) — 새 데이터가 필요 없다.
  ② **그 표현으로 말하기**(`renderSay`) — 고른 프레임은 내 말풍선에 초록으로 박히고(`.fill-fixed`) 남은 자리만 뱅크에서 탭해 문장을 완성한다. 완성된 문장이 내 대사로 남는 것이 '말해봤다'의 실체다. 말풍선은 카드보다 좁아 종결부호만 다음 줄로 떨어지므로 **마지막 칸과 부호를 `.fill-nw`로 묶어** 함께 넘긴다.
  지시문은 한국어지만 generic 하다(`어떤 표현으로 답할까요?` → `그 표현으로 말해 보세요`) — 정답의 번역이 아니다. 오답 칩을 고르면 그 칩만 붉게 표시하고 A 해석은 계속 잠긴다. 정답이면 `✓ 좋아요!` + `의미`(내 답의 한국어) + `ⓘ 쓴 표현`(프레임·뜻)을 열고 정답 선택지를 초록으로 표시한다(비교는 `data-ok`로 — 마이크 아이콘이 섞인 `textContent`로 비교하면 빗나간다). **자유 입력 버전도 나란히 둔다** — 능동적 인출 감각은 선택으로는 안 나오기 때문이다. 아무 단서 없이 쓰게 하면 막히고 다 주면 인출이 아니므로 **지원을 한 칸씩만 여는 힌트 사다리**를 쓴다([1] 어휘의 `힌트 보기 → 정답 보기` 규약과 같다): `배운 표현 목록 → 이 상황의 표현 하나 → 첫 단어 → 정답`. 채점은 프레임 포함 여부로만 하므로 모범 답안과 다른 문장(`Hey, you should get up already!`)도 통과한다. 그때 `의미`는 **내가 말한 문장의 뜻**이어야 하므로 모범 답안의 한국어를 거기 붙이지 않고 `ⓘ 이 상황에선 이렇게 말해요`로 따로 뗀다.
  두 방식은 홈 `상황만 풀기` 카드의 **`골라 답하기` / `직접 입력` 두 진입점**으로 각각 들어간다(세션 중 [5]는 `ve:opts.sitType`을 따른다).
  **홈의 보조 진입점(`짝 문장 문항만` · `상황만`)은 표본을 보는 모드다**(`sampleMode`) — 끝까지 풀어도 본 세션의 진행 위치(`ve:pos`)를 저장하지 않고, 끝 화면의 `처음부터 다시`도 그 모드로 되돌아간다.
  **[5]는 3문항마다 나오므로(`REVIEW_EVERY`) 한참 풀어야 볼 수 있다 — 홈에 `상황만 풀기` 별도 진입점**(`startSituationOnly`)을 둬 상황이 붙은 문항만 연속으로 풀 수 있다. 이 모드에선 `sitOnly=true`이고 `buildStepIndex`가 문항당 1단계로 계산한다. **개발 확인용으로 `devPanel`이 카드 아래 붙어** 이 상황이 어떤 표현으로 만들어졌고 오답이 어디서 왔는지 보여준다(점선·모노 표기로 학습 UI 와 구분, `sitOnly`에서만).
  **A 대사는 소스 CSV에 없어 `scripts/vocab_expression_situations.json`에 손으로 적었다**(학습 문장 `sentence.en`을 키로 하는 `{a_en, a_kr, hint_en}`, 현재 30개. `hint_en`은 자유 입력 버전용 간접 안내로 지금은 화면에 쓰지 않는다). 빌드가 `situation`으로 붙이고, **구간에 상황이 붙은 문항이 없으면 이 단계를 건너뛴다**.
- `scripts/vocab_expression_questions.json` — 그 문장이 자연스러운 짧은 답이 되는 A 대사를 **영어 문장(키)마다** 손으로 적은 것(`[영어, 한국어 해석]`, **562개**). 빌드가 학습 문장에는 `ask`로(222문항), 형제 문장에는 `apply[].ask`로(340개) 붙이고 **[4]의 자극 `영어 질문`·`대화 한가운데`가 후자를 쓴다**. 콘텐츠 검수 전 문안이며, **질문에 프레임 조각이 다 들어가면 [4]의 답이 새므로 그건 다시 써야 한다**(현재 0건 — `have … in common`처럼 실제로 걸린 것들을 고쳤다).
- `scripts/vocab_expression_replies.json` — `[4]`의 자극 `대화 한가운데`에서 **내 말 뒤에 오는 A의 반응**을 영어 문장(키)마다 손으로 적은 것(`[영어, 한국어 해석]`, **261개**). 빌드가 `apply[].reply`로 붙이고, `dialogTarget`은 `ask`와 `reply`가 **둘 다** 있는 형제만 대상으로 삼는다(261/261 성립). 반응은 **내용어를 고정하지 않는다** — `그거 좋네, 파스타로 하자`처럼 특정 답을 전제하면 내 말이 하나로 닫히고, 프레임 낱말이나 표현 뜻의 한국어를 넣으면 답이 샌다.
- `public/vocab-expression.html` — **별도** 어휘→표현 프로토타입(자체 완결형 HTML, 본 앱과 무관, `/vocab-expression.html`로 서빙). **앱(말해보카)의 어휘 학습 UI·UX를 화면 그대로 재현**(참고 영상 기반). 흐름은 문항마다 **[1] 어휘 빈칸 → [2] 표현 넣기 → [3] 문장 전체 → [4] 말해보기** 4단계이고([2]·[3]은 **한 화면의 두 박자**로, 처음부터 끝까지 **같은 학습 문장**을 쓴다), `[5] 상황`이 `REVIEW_EVERY`(3)문항마다 끼어든다(옵션). `buildStepIndex`가 총 단계 수를 합산하고 `setProgress`는 **문항 내 단계 위치**만 받는다. 상단바는 트로피·진행바(별 마커)·`i / N`·설정.
  **옵션은 세 개다** — `[5] 상황 카드 넣기`(`ve:sit`, 기본 ON) · `[4] 말해보기 자극`(`ve:spk`, `SPK_MODES` kr·en·dlg, 기본 kr) · `[4] 말해보기 방식`(`ve:spkv`, `SPK_VARS` o·h·a·c, 기본 a). 두 축 모두 라벨만으로는 무엇이 다른지 읽히지 않으므로 **한 줄 설명을 함께 세운다**(`.seg.seg-v`). **옵션을 바꾸면 그 자리에서 바로 적용된다**(`curStep`·`applyStepOption`) — 설정을 만지고 홈으로 나갔다 다시 들어와야 하면 무엇이 달라졌는지 감각이 끊긴다. 기어 팝오버는 `stage` 밖이라 화면을 다시 그려도 열린 채 남는다. 다만 다시 그리는 것은 **`[4]`뿐**이다 — 배열 도중에 화면을 갈아엎으면 풀던 것이 날아간다. 홈 `옵션` 박스와 상단 기어 팝오버 양쪽에서 토글하고, 켜고 끌 때 `buildStepIndex`를 다시 돌려 진행바 기준을 맞춘다. 칩 강조·단계 배너·이동 연출·예고 말풍선 같은 실험 스위치는 두지 않는다 — 선택지가 많아지면 무엇을 보고 있는지가 흐려진다. **2박자의 방식만 진입점으로 갈린다**(`ve:flow`, `patternStep`): 홈의 큰 CTA는 `transfer`(문장을 통째로 배열), 그 아래 `흐름 1로 풀기` 카드는 `expand`(프레임을 굳히고 나머지만 자란다). 그 밖의 단계는 같아서 비교가 2박자 조작 하나로 좁혀진다.
  **두 버전을 바꿔가며 비교한다** — 어휘 학습 문장을 무엇으로 쓰는지만 다르고 이후 단계는 동일하다:
  - **버전 A** — 어휘는 어휘 예문(`vocabA`)으로 배운다(현재 앱 구조).
  - **버전 B** — **표현 문장 자체**(`vocabB`)에서 트리거 어휘를 빈칸으로 배운다.
  - 진행 위치(`ve:pos`)는 **두 버전이 공유**한다 — 버전만 바꿔 같은 문항을 바로 비교할 수 있게. 홈 배치는 `탭 → 시작 카드(흐름·남은 문항 + 큰 CTA `.start-btn`) → 옵션`. 레벨은 문항 카드 좌상단 태그(`레벨 N`, gse 어휘 레벨)로만 표시한다.
  - **[1] 어휘 빈칸(`renderVocab`)** — 채팅 말풍선 카드(좌상단 `레벨 N` 태그·우상단 `NEW` 배지·상단 파란→흰 그라데이션): 한국어 번역(정답 뜻 영역 초록 `.tgt`, 괄호 설명줄 muted) 위, 영어 예문의 `[단어]`를 **인라인 입력 빈칸**(`<input class=blank>`, `field-sizing:content`로 자동 폭)으로 보여준다. 하단은 `? 힌트 보기` / `🎤 음성 모드`(비작동). 힌트는 **순차** — `? 힌트 보기`(**앞 2글자**+나머지 길이) → `✔ 정답 보기`(정답 전체, `.blank.revealed`). 둘 다 **placeholder로** 보여주므로 입력값이 남아 있으면 가려져 아무 반응이 없는 것처럼 보인다. 그래서 **정답 보기를 누를 때만 입력값과 오답 박스를 지워** 정답 placeholder가 보이게 하고(힌트 보기는 입력값을 유지), 그 뒤 다시 타이핑해도 정답이 가려지지 않게 **문장 아래에 `정답 {단어}` 주석 한 줄**(`#revealNote`)을 남긴다. **정답 입력 시 확인 없이 즉시 처리**(`norm`), 오답은 **카드 안 파란 박스**에 `{입력} - 정답과 N글자 달라요`로 틀린 글자 빨강(`diffSpell`). 정답이면 카드가 초록으로 바뀌고 ✓ → 카드 안에 액션 줄이 붙고 하단이 단일 CTA로 교체된다(`resultActionsHtml`·`arrangeBottomHtml`). 버전 B는 표현 문장 번역에 `[뜻]` 마크업이 없으므로 `mark_meaning_kr`가 **뜻 조각을 번역문에서 직접 찾아 `[..]`로 감싼다**(`[옷] 입어.`). **어휘 CSV 예문 번역에 저작된 `[뜻]`을 사전 뜻보다 먼저 시도한다** — 문맥에 맞는 구어체라 더 잘 맞는다. 못 찾는 경우(`bad` → `별로였어?`)만 **`빈칸 뜻 {뜻}` 줄**(`.b-hint`)로 대체한다.
  - **표현 산출을 문장 배열보다 먼저 놓는다** — 문장을 통째로 배열하면 그중 무엇이 재사용할 표현인지 알려주는 정보가 화면에 하나도 없다. 배열만으로는 과제가 성립하지 않는다는 점도 같이 고려했다 — 프레임 단어 수는 2단어 161문항(62%)·3단어 77·4단어 17·5단어 6으로, 2단어 프레임은 가능한 순서가 2가지뿐이다. **그래서 첫 학습에는 함정을 넣지 않는다** — 첫 학습의 정오답으로 유닛 습득을 판정하지 않고, 1~3일 뒤 함정을 섞어 다시 노출해 그때 복습주기를 정하는 것이 설계다(그 복습 판정 단계는 아직 구현하지 않았다).
  - **[2]·[3] 표현 넣기 → 문장 전체(`renderPattern`, 한 화면 두 박자)** — **두 박자 모두 같은 문장**(트리거 어휘가 든 학습 문장)을 쓴다. 1박자는 변하는 부분(슬롯)을 회색(`.fill-given`)으로 준 채 **프레임 자리만** 채우게 하고(타이틀 = 표현 뜻 초록 `.tgt`, 문장 해석은 영어 아래 `.sent-kr`), 2박자는 **같은 문장을 표현까지 포함해 통째로** 배열한다. 타이틀이 문장 뜻으로 바뀌는데, 표현 뜻을 따로 적지 않고 **문장 뜻 안에서 표현에 해당하는 구간만 초록**으로 표시한다(`sentence.krMark` → `sentTitleHtml`) — `[1]` 어휘가 번역문에서 정답 뜻만 초록으로 보여주는 것과 같은 표기다. 구간은 빌드가 찾는다(`mark_expr_kr`). 표현 뜻은 `~`로 슬롯이 표시돼 있고 종결형이 문장마다 다르므로 세 단계로 좁힌다: ① 조각·어간을 어휘 뜻보다 넓게 펼쳐 **글자 그대로**(`expr_meaning_spans`, `~을 입다` → `옷 [입]어.`) → ② **어간 끝 음절의 활용을 무시하고**(`syllable_class` — 받침만 무시해 `켜`↔`켤`, 2음절 이상이면 모음까지 무시해 `치우`↔`치워`·`돌려주`↔`돌려줘`) → ③ 그래도 없으면 **용언 어간 1글자**를 낱말 첫머리에서만(`expr_stem_spans`, `켜다`→`켤게`). ③을 어간으로 한정하는 이유는 1글자 조사·파편을 넣으면 엉뚱한 구간을 칠하기 때문이다(`~만 빼고`의 `만`이 `많`을, `~이 기대돼`의 `이`가 `있`을 잡는다). **후보 정렬 키는 결정적이어야 한다** — 길이만으로 `set`을 정렬하면 같은 입력에서도 빌드마다 `krMark` 구간이 달라진다(`PYTHONHASHSEED`로 재현된다). `expr_meaning_spans`는 `(-길이, -출처 낱말 인덱스, 등장 위치, 문자열)`, `expr_stem_spans`는 `(-길이, 등장 위치, 문자열)`로 묶는다. `EXPR_PARTICLE`에는 `도`도 들어간다 — 없으면 `~도 ~도 둘 다`에서 `둘 다`가 한 덩어리 후보로 서지 못한다. **207/261(79%)**에서 찾고, **못 찾은 54개만** 표현 뜻을 그 아래 초록 칩(`.expand-mean` > `.rv-chip.fr`)으로 대체한다 — 나머지는 `~와 이야기하다` / `부모님께 말씀드려 볼게.`처럼 번역이 동의어를 쓴 경우라 문자열로는 못 잇는다(`[1]`의 `빈칸 뜻` 대체줄과 같은 구조). 지시문은 두지 않는다 — 연결은 초록 구간과 초록 타일이 맡는다. **[1]에서 배운 어휘는 1박자 화면에 이미 보인다** — 회색으로 주어진 부분에 들어 있다(261문항 전부 확인).
    2박자에서 프레임을 박아 두고 나머지만 배열하게 하면 자리만 채우는 과제가 되므로 **프레임도 뱅크에 올린다**. **단어 카드는 항상 낱말 단위다**(덩어리 타일은 쓰지 않는다) — 대신 프레임 낱말을 초록(`.tile.fr`·`.frame-w`)으로 표시해 그중 무엇이 방금 익힌 표현인지 계속 보이게 한다.
    **화면을 교체하지 않는 것이 핵심이다** — 교체하면 2박자가 새 문제로 읽히고 1박자에서 이어졌다는 감각이 끊긴다. 같은 카드에서 타이틀만 바뀌고 줄이 `.grow-in`으로 다시 열린다. 결과창은 2박자 끝에 한 번만(`patternPayoffHtml`) 붙는다.
    **전환의 순서가 '완료 → 재료 도착'으로 읽혀야 한다** — 내가 놓은 타일을 줄에서 뱅크로 끌어내리면(FLIP으로 만들어 봤다) 그 방향이 곧 '회수'라서 부정적으로 읽힌다. 리듬은 ① 놓은 프레임이 초록으로 **팝**(`.ok-pop`, 0.28s)해 1박자 완료를 먼저 인정 → ② 190ms에 줄과 뱅크가 **제자리에서** 흐려짐(`.fade-out`) → ③ 400ms에 내용을 바꾸고 줄은 **아래에서 올라오며**(`.fill.pre-in` 해제) 뱅크는 타일마다 `--i` 스태거로 올라온다(`.bank-in`). 아무것도 위에서 끌려 내려가지 않는다.
    매끄러움의 조건은 **한 요소에 한 가지 메커니즘**이다 — 컨테이너 페이드와 칸마다의 `.grow-in`을 겹치면 두 동작이 어긋나 보이므로 2박자 칸에서는 `.grow-in`을 뺐다(흐름 1은 칸이 *추가*되는 것이라 그쪽만 유지한다). 나가는 방향(제자리)과 들어오는 방향(아래→위)을 갈라 둔 것도 같은 이유다. `pre-in` 해제는 rAF 두 번 + **90ms setTimeout 백스톱**을 함께 쓴다 — 탭이 숨겨지면 rAF가 멈춰 클래스가 남고 줄이 안 보인다. 전환 중 중복 입력은 `switching` 플래그로 막고, `정답 보기`로 넘어갈 때도 **줄을 먼저 채운 뒤**(`renderFill`) 전환해야 팝을 걸 타일이 있다. 전환은 타이머로 예약되므로 **콜백마다 `fillEl.isConnected`를 확인한다** — 예약된 채로 화면이 교체되면 이전 타이머가 새 화면의 타이틀·뱅크를 덮어쓴다(스윕에서 실제로 잡혔다). 위치를 셀렉터(`.fill-word, .fill-given, .fill-blank`)로 순서만 맞춰 얻는 이유는 `withPunct`가 마지막 칸을 한 겹 감싸 자식 순회가 어긋나기 때문이다. 흐름 1은 사라지는 것이 없어(프레임이 그대로 남는다) 이 연출이 필요 없다.
    **1박자에서 표현을 못 떠올렸을 때는 오답노트 팁을 연다**(`.nb-tip`, 뱅크와 버튼 사이). 오답을 확인했을 때와 `표현 보기` 버튼을 눌렀을 때가 **같은 동작**이다 — 표현과 그 뜻을 한 줄로 짚어 주고(`feel like : ~ 같아 / ~인 것 같아`) **줄을 비운다**. **대신 채워 주고 넘어가지 않는다** — 팁을 보고 유저가 직접 배열해야 2박자로 간다. 못 떠올린 경우에도 결국 손으로 한 번은 만들게 되고, 그래야 표현을 산출했다고 할 수 있다. 팁은 **1박자의 것이라 2박자로 넘어가며 닫힌다**. 팁의 표기는 `exprLabel`이 `form`의 자리표시자를 걷어내 **인용 라벨**로 줄인 것이다(`feel like ______` → `feel like`, `put ______ on` → `put … on`, 자리표시자를 품은 괄호는 통째로 뺀다). 1박자 버튼 라벨이 `표현 보기`이고 2박자에서 `정답 보기`로 바뀌는 것도 같은 이유 — 1박자에서 공개되는 것은 정답 문장이 아니라 표현이다. **2박자의 오답은 기존대로** 붉게 표시한 뒤 0.7초 후 정답으로 교정하고 결과로 간다(팁 없음).
    **배열 단위는 조각(`baseSegs`)이 아니라 문장 토큰**(`tokenize` + `frameFlags`)이다 — 조각을 공백으로 자르면 `, grandpa` 같은 슬롯에서 **쉼표만 든 타일**이 생긴다. 토큰은 `time,`처럼 부호가 낱말에 붙어 자연스럽고, 어느 토큰이 프레임인지는 조각을 순서대로 소진해 표시한다. 연속한 비프레임 토큰은 `slotRuns`가 한 덩어리로 묶어 1박자의 회색 단위가 된다.
  - **흐름 1(`renderExpand`)** — 같은 문장·같은 두 박자를 쓰지만 **2박자에서 프레임을 다시 배열하지 않는다**: 1박자에 놓은 프레임이 `.locked`로 그 자리에 남고 나머지만 그 위로 자란다. 1박자에는 슬롯 자리를 아예 안 보여주고 **프레임 조각 사이에만 `···` 칩**(`.fr-slot`)을 둔다 — 표현 앞뒤의 슬롯까지 표시하면 `get up`이 `~ get up`처럼 보여 표현이 아닌 걸 배우는 것처럼 읽힌다(앞 156·뒤 195문항).
  - **[4] 말해보기(`renderSpeak`)** — [2]·[3]은 주어진 문장을 만드는 일이라 "이 표현을 내가 쓸 수 있다"까지 가지 않는다. 효용감은 **배운 표현으로 처음 보는 문장을 내가 세웠을 때** 나온다(어학 수업에서 "`~의 앞에`를 배웠으니 '내 앞에 다나카상이 있습니다'는?" 하고 묻는 그 자리다). 그 수업에서 답이 되는 이유는 **나머지 낱말을 이미 알기 때문**이지 자유 영작이라서가 아니다. **공통 구조는 문장 전체를 매번 직접 타이핑하는 것**이다(탭 배열인 `c`만 예외) — 문장 틀을 주고 표현 자리만 채우게 하면 관습적으로 방금 본 표현을 끼워 넣는 한 칸이 되고, 문장을 내가 세운 감각이 남지 않는다.
    **화면은 두 축으로 갈린다.** 라벨만으로는 무엇이 다른지 읽히지 않으므로 두 축 모두 **한 줄 설명을 함께 세운다**(`.seg.seg-v`). 기본은 `한국어 문장` × `섞어서 준다`.
    - **자극**(무엇을 보고 말하나, `ve:spk`·`SPK_MODES`) — `kr` **한국어 문장**(만들 문장의 뜻을 한국어로 준다) · `en` **영어 질문**(A가 영어로 묻고 영어로 답한다) · `dlg` **대화 한가운데**(앞뒤 대사가 정해져 있고 가운데 내 말만 비어 있다).
    - **방식**(무엇을 주나, `ve:spkv`·`SPK_VARS`) — `o` **안 준다** · `h` **줄어든다** · `a` **섞어서 준다**(기준선) · `c` **탭해서 배열**.
    **자극이 화면 골격을, 방식이 재료를 정한다** — `renderSpeak`이 자극으로 먼저 갈라 화면을 고르고(`dlg`→`renderSpeakDialog`, 그 밖에는 `o`→`renderSpeakOpen` / `h`→`renderSpeakSheet` / 기본 재료 화면), 그 안에서 `spkGives`가 재료를 낸다. 12칸 중 **`dlg×c`만 뱅크 화면으로 폴백한다** — 탭 배열은 뱅크가 하단을 다 써서 앞뒤 대사를 함께 세울 자리가 없다.
    재료는 세 헬퍼가 공유한다: `spkGives(item, t)`가 `kind`(`none`/`ordered`/`shuffled`)와 낱말을 내고, `spkGivesHtml`이 그것을 그리며(`ordered`는 조각 칩 `.sh-give`, `shuffled`는 `함께 쓸 낱말` `.sp-mat`), `spkMatch(typed, allowed)`가 다중집합 + 오타로 채점한다. **네 방식이 전부 이 셋을 쓴다** — 방식마다 재료를 따로 계산하면 같아야 할 것이 조용히 갈라진다.
    **카드 머리도 한 곳에서 나온다**(`spkHeadHtml`·`spkStimHtml`·`spkTaskHtml`) — 방식은 재료만 바꾸므로, 방식을 바꿨는데 표제·지시문·대상 문장의 표기까지 달라지면 **무엇을 비교하고 있는지가 흐려진다**. 실제로 `줄어든다`만 표제를 걸고 `섞어서 준다`는 안 걸어, 두 칸을 오갈 때 재료가 아니라 화면이 바뀐 것처럼 읽혔다. 순서는 모든 칸에서 **표제(`이번에 배운 표현` + 뜻) → 지시문 → 자극 본문 → 팁 → 재료 → 답 자리**다. 표제에 거는 것은 **뜻뿐**이고 영어 철자는 사다리의 `표현 보기`에서만 열린다. 지시문 문구는 자극이 정한다(`이 문장을 만들어 보세요` / `답해 보세요` / `가운데를 채워 보세요`) — `안 준다`만 대상 문장이 없어 `하고 싶은 말을 만들어 보세요`로 갈린다.
    **한국어 대상 문장은 표현 구간만 초록으로 칠한다**(`sentTitleHtml`, `krMark`) — `[2]`·`[3]` 타이틀과 **같은 함수·같은 표기**다. 뜻만 통째로 주면 *이 표현을 쓰라*는 신호가 화면에 없어 `넘어가죠.`를 보고도 방금 배운 `move on`이 떠오르지 않는다. 카드 머리의 표현 뜻(`다음으로 넘어가다`)과 문장 속 구간(`[넘어가]죠.`)이 같은 초록으로 이어지는 것이 그 신호다. 구간을 못 찾은 경우(`apply` 기준 343/1441)는 머리의 뜻만 남는다.
    **지원 사다리의 첫 칸은 언제나 `표현 보기`**다 — 막혔을 때 가장 먼저 필요한 것이 표현이고, `[2]` 화면에서 표현을 못 떠올렸을 때 오답노트 팁을 여는 것과 **같은 규약**이다(`.nb-tip`, 같은 `nbTipHtml`). **단, 앞 줄에 내가 쓴 답이 이미 있으면 뺀다**(`h`의 두 줄째부터) — 표현이 거기 초록으로 보여 같은 것을 두 번 주게 된다. 팁은 **답을 제출해 표현이 안 보일 때 자동으로도 열리고**, 그때 사다리 첫 칸을 쓴 것으로 친다(같은 지원을 두 번 세지 않는다). 화면에서 지원은 **답 자리 위에 위에서부터 팁 → 재료 → 답 자리** 순으로 쌓인다.
    **답 자리는 한 줄 입력칸이 아니라 낱말 한 칸씩이다**(`.sp-slot`). 재료를 주는 방식에서는 답의 낱말 수가 재료 + 표현으로 이미 정해져 있어 칸을 미리 그려도 새로 새는 정보가 없고, 대신 '몇 낱말을 어디까지 놓았나'가 눈에 보인다. **`o`는 칸 수를 미리 정하지 않는다** — 거기서는 답의 범위 자체가 유저의 것이라 칸이 치는 만큼 자란다. 실제 타이핑은 칸 위에 깔린 투명 입력(`.sp-catch`)이 받으므로 모바일 키보드·데스크톱 타이핑이 그대로 동작하고, 현재 칸에 캐럿이 깜빡인다. 칸보다 많이 쓰면 넘친 칸을 붉게 덧붙인다(`.sp-slot.extra`). 정답 후에는 그 칸들이 그대로 **완성 문장의 낱말 타일**이 되고 프레임만 초록이다(`.sp-tile.fr`, 조각 정보가 없는 자유 답안은 `frameWordFlags`가 굴절을 허용해 찾는다).
    ### 자극
    - **`kr` 한국어 문장** — 카드 머리에 표현 뜻, 그 아래 만들 문장의 한국어(표현 구간 초록). 대상 문장은 `speakTarget`이 고른다.
    - **`en` 영어 질문** — A가 영어로 묻고(`apply[].ask`, 한국어 해석 동반) 내가 영어로 답한다. 한국어 문장을 주지 않으므로 **영어 → 영어 인출**이 된다. 질문에 프레임 조각이 다 들어가면 답이 새므로 그런 문안은 다시 쓴다(현재 0건).
    - **`dlg` 대화 한가운데**(`dialogTarget`·`renderSpeakDialog`) — A의 앞 대사와 **내 말 뒤 A의 반응**이 모두 정해져 있고 **가운데 내 말만 비어 있다**. 앞 대사만 주는 버전은 사실상 `en`과 같았다 — 뒤가 비어 있으면 내 말이 어디로 이어지는지가 화면에 없어 그냥 영어 질문에 답하는 일이 된다. 뒤 대사(`.sit-turn.dlg-after`)는 풀는 동안 흐려져 있고(`opacity:.42`) 통과하면 `.on`으로 켜진다 — **내 말이 그 반응을 끌어냈다**는 것이 이 자극의 산출물이다. 앞뒤가 다 있는 형제만 대상이 되고(`a.ask && a.reply`), `speakTarget`의 결과를 먼저 쓰고 없으면 재료 적은 것 → 짧은 것으로 고른다. **261/261에서 성립한다.** 재료가 있으면 다중집합으로 채점하고 칸 수를 고정하며, 없으면 열린 채점(프레임 + 프레임 밖 1낱말 + 중복 아님)이다. 제외 목록은 `o`보다 느슨하다 — **대상 문장 그대로가 이 질문의 가장 자연스러운 답**이라 그걸 막으면 맞는 답을 틀렸다고 하게 된다.
    ### 방식
    - **`o` 안 준다**(`renderSpeakOpen`) — **화면에 재료가 없다.** 표현 뜻과 빈 줄뿐이고, 이 표현으로 무슨 말을 할지는 유저가 정한다.
      **답을 닫으려고 재료를 화면에 놓는 순간 과제가 조립이 된다** — 낱말을 다 주는 연습지든 섞어 주는 재료든 뱅크든 후보 목록이든 같은 실패였다. 답을 닫으려고 준 것이 매번 답을 대신 써 줬다.
      기준이 된 와우는 일본어 수업에서 `~の上に ~があります`를 배우고 **"앞에 있는 거라면 뭐든 말할 수 있겠구나"**를 느낀 순간인데, 그것은 *한 문장을 맞힌 것*이 아니라 **아무거나 된다는 자각**이다. 거기서 무료였던 것은 내용(츠쿠에·혼)이고 일거리는 구성이었다. **우리 데이터는 한 문항에서 어휘와 표현이 동시에 새로 들어와** 그 구조를 데이터로 만들 수 없다 — 앞서 배운 어휘가 이 프레임 슬롯에 들어가는 문항은 **59/261**(2개 이상은 5)뿐이고, 반대로 **자기 `[1]` 어휘가 자기 슬롯에 있는 문항은 221/261**이다. 그러면 이 시점에 진짜로 무료인 것은 **유저 머릿속의 어휘 전체**다 — 앱이 목록으로 줄 수 없는 것. 그래서 아무것도 주지 않는다.
      채점 조건은 셋이고 **내용은 완전히 자유**다: ① 배운 표현을 썼는가(`framePos`) ② 표현 밖 낱말이 하나라도 있는가 — 표현만 치고 넘기는 것을 막는다(프레임이 문장 전체인 문항은 0/261이라 항상 성립한다) ③ 앞에서 이미 만든/본 문장이 아닌가 — 베껴 쓰기를 막는다. **틀린 영어가 통과할 수 있는 것이 이 채점의 상한**이고(`[5]` 자유 입력도 같다), 실시간 LLM 없이는 막을 수 없다.
      **와우는 통과 뒤에 온다.** ① 내가 쓴 문장이 예문들과 **같은 목록의 첫 줄**로 선다(`.pf-cmp.mine` + `.op-badge`) — 학습 문장과 내 문장 두 줄은 "같은 표현"까지만 보여주고 "뭐든 된다"는 안 보여준다. 목록을 `내 것 / 예문`으로 가르지 않는 것이 핵심이다(예문이 1개 이상인 문항 261/261, 3개 이상 251이라 목록은 4~10줄이 된다). ② **예문에 없는 낱말을 썼으면 그것을 짚어 인정한다**(`.op-new`, 기능어와 세 글자 미만은 뺀다) — 앱이 몰랐던 문장을 내가 만든 것이고, 그게 감점이 아니라 칭찬이면 다음 문항부터 일부러 새 낱말을 밀어 넣어 본다. 그 순간이 "뭐든 말할 수 있겠구나"다.
      사다리는 `표현 보기` → `예문 보기` → `정답 보기`이고, **본 예문은 제외 목록에 들어간다**(보고 베끼면 산출이 아니다). 마지막 칸만 그 제외를 풀고 칸까지 채워 준다 — **탈출구가 없으면 진행이 끊긴다.** 그 예문은 **그 자신이 이 화면의 채점을 통과하는 문장**이어야 하므로 두 가지를 걸러낸다(`egOk`): 형제 문장이 **표현 그 자신**인 경우(`Have fun!`·`How come?`)와, 불규칙 활용이라 `framePos`가 표현을 못 집는 경우(`get home` → `I got home`). 그 다음에 짧은 것을 쓴다. 261문항 전부에서 걸러진 뒤에도 후보가 남는다.
    - **`h` 줄어든다**(`renderSpeakSheet`) — 어학 교재의 연습지 형태다(`NEW 다이나믹 일본어` 9과 `형용사+の` 연습지). 같은 표현으로 문장을 **연달아** 쓰고, 쓴 줄이 위에 쌓인다(`.sh-row.mine`). 연습지가 다른 것은 **같은 표현을 자리를 바꿔 여러 번 쓴다**는 점이고(`put the TV on` / `put something funny on`), 각 문장에 필요한 **낱말은 다 준다**는 점이다(연습지의 `(派手な)` 표기) — 그래서 떠올릴 것이 표현 하나로 좁혀진다.
      **기본은 한 줄이다.** `[4]`는 `[2]`·`[3]`을 지나 온 마지막 단계라, 여기서 여러 줄을 고정으로 시키면 **잘 넘긴 유저에게 벌을 주는 셈**이 되고 단계가 지루하게 길어진다. 반복은 **막힌 유저에게만** 붙인다: 그 줄에서 **지원을 열었거나 오답을 냈으면**(`struggled`) 같은 표현으로 한 줄이 더 열리고, 그 줄에서도 막히면 후보가 남아 있는 만큼 또 열린다(`sheetRows`, 최대 3). 덧붙은 줄에는 **`한 번 더` 칩**(`.sh-rv.again`)으로 이유를 적는다 — 왜 한 줄이 더 생겼는지 화면이 말해 주지 않으면 그냥 길어 보인다. 지원 없이 넘기면 **261문항 전부 1줄**, 한 번 막히면 258문항이 2줄(후보 1개뿐인 3문항은 막혀도 1줄), 매 줄 막히면 251문항이 3줄이다. **한 줄로 끝나면 번호를 붙이지 않는다**(`numbered`) — 한 줄짜리에 `1.`이 붙으면 없는 2번을 찾게 된다.
      **화면에 답이 미리 있으면 안 된다** — 표제에 `put … on`을 박고 1번 줄에 학습 문장을 채워 두면 표현도 모범 답안도 눈앞에 있어 **보고 옮겨 적는 일**이 된다. 그래서 **예시 줄을 두지 않는다**. 일본어 연습지의 제목(`형용사+の`)이 그대로 답이 되지 않는 것과 같은 자리다.
      쌓인 줄과 지금 쓰는 줄은 **크기로 갈린다** — 지난 줄의 한국어는 `.sh-kr`(작게), 지금 줄은 다른 방식과 같은 `.sp-kr`(크게)다. 첫 줄은 재료 화면과 같은 자리에서 시작해야 한다(`.sh-rows > .sh-row:first-child`) — 방식을 바꿀 때 줄이 튀면 바뀐 것이 재료가 아니라 화면으로 읽힌다.
      **이 방식의 이름은 지원이 줄어드는 것에서 온다**(`aidLevel`, `ve:aid`). 산출물에서 효용감을 찾는 것은 여러 번 실패했다 — 문장을 하나 만들든 넷을 만들든, **매 문항 앱이 해주는 양이 같으면 내가 나아졌다는 근거가 화면에 없다**. 그래서 주어지는 낱말을 세 단으로 두고 **문항마다 옮긴다**: **0단** `segs`의 슬롯 조각을 **문장 순서대로**(`.sh-give`) · **1단** 조각을 **낱말로 쪼개 섞어서**(`.sp-mat.sh-mat`, `함께 쓸 낱말` — `a`의 재료 줄과 같은 표기) · **2단** 주지 않고 사다리에 **`낱말 보기`**를 끼워 한 칸으로 되돌릴 수 있게 한다(지원을 없애 놓고 되돌릴 길을 두지 않으면 졸업이 아니라 벌이다). 조각을 낱말로 쪼개면 `, though`에서 **쉼표만 든 칩**이 생기므로 알파벳·숫자가 없는 조각은 버린다.
      **`한 번 더` 줄과 같은 신호를 반대 방향으로 쓴다** — 막히지 않고 넘긴 문항이면 한 단 내려가고(앱이 덜 해준다), 막히면 한 단 올라간다. 두 문항을 깨끗하게 넘기면 낱말을 아예 안 주는데도 쓰게 되고, 그 **화면이 비어 있는 것**이 곧 근거다. 문구로 말하지 않는다. 세션을 시작하면 0단으로 되돌린다(`setAid(0)`).
      후보는 **짧은 것부터** 쓴다(`sheetRows`) — 첫 줄에서 표현의 모양을 잡고 다음 줄에서 자리를 바꿔 본다. 데이터는 `apply[]`(조각 정보가 붙어 있는 형제 문장)를 쓴다. **자극이 A 대사를 쓰면 대사가 있는 후보를 앞세운다**(`speakTarget`의 ⓞ 규칙과 같다) — 그러지 않으면 연습지 칸에서만 자극이 움직이지 않아 `영어 질문`이 `한국어 문장`과 같은 화면이 된다(대사 보유 후보가 1개 이상인 문항 261/261이라 첫 줄에는 늘 말풍선이 선다). 카드가 길어지므로 **머리 그라데이션을 px로 고정한다**(`.chat-card.sheet`) — `%`는 카드 키를 따라가서 첫 줄까지 물들고 '이 줄만 맞았다'처럼 읽힌다.
    - **`a` 섞어서 준다**(기준선) — 낱말을 **섞어서** 주고(`.sp-mat`, `함께 쓸 낱말`) 표현은 감춘 채 **한 문장만** 쓴다. `h`와의 차이가 곧 비교 축이다: 낱말을 조각째 순서대로 주는가 낱말로 쪼개 섞는가, 그 양이 문항마다 줄어드는가, 한 문장인가 막히면 여러 문장인가.
      입력한 낱말은 **칩에서 죽인다**(`.mat.used`, 배열 화면의 `.tile.placed`와 같은 규약) — 이 줄의 용도는 '무엇을 썼나'가 아니라 **'아직 뭐가 남았나'**이므로 남은 것만 진하게 둔다. 같은 낱말이 둘이면 둘 다 써야 둘 다 꺼진다(다중집합으로 소진). 정답 후에는 역할이 끝나 숨긴다. 사다리는 `표현 보기` → `첫 단어 보기` → `정답 보기`다.
    - **`c` 탭해서 배열**(`renderSpeakArrange`) — 낱말을 탭해 배열하고 뱅크에 **함정이 섞인다**(`item.traps`에서 대상 문장에 없는 낱말 최대 2개, 새 저작 없음). `함께 쓸 낱말`이라면서 안 쓸 낱말을 주면 화면이 거짓말을 하지만, **배열 뱅크는 애초에 "이 중에서 고르라"는 약속**이라 그 문제가 없다 — 그래서 재료 줄 대신 `[2]`·`[3]`과 같은 `.bank`를 쓴다. **뱅크의 프레임 타일을 초록으로 칠하지 않는다** — 어느 것이 방금 배운 표현인지 알아보는 것이 과제라, 표시해 주면 과제가 사라진다. **타일 글자는 모두 소문자로 내린다** — 문장 첫 낱말만 대문자면 어디가 문장 머리인지가 새므로 배열 과제가 반쯤 풀린다. 놓은 카드는 눌러 되가져온다(`.sp-slot.on`). 뱅크에 낱말이 다 있어 `첫 단어 보기`는 의미가 없으므로 사다리는 `표현 보기` → `정답 보기` 둘이다. 남아 있는 트레이드오프는 **타이핑이 없다**는 것이다.
    **방식을 바꾸면 `renderSpeak`이 다시 갈라야 한다** — 연습지나 배열 화면이 `curStep`을 자기 자신으로 잡으면 옵션을 바꿔도 같은 화면이 다시 그려진다. 모든 방식이 `curStep=renderSpeak`을 쓴다.
    지금까지 기각된 시도와 그 이유는 다음 설계의 제약이다:
    - 재료를 감추고 힌트로 막힌 칸을 하나씩 채우기 → **너무 막막하다**. 답이 하나로 닫혀 있는데 화면이 그 사실을 안 알려주면, 다른 낱말이 생각날 때마다 헛돈다
    - 방금 만든 문장을 옆에 두고 낱말만 바꿔 한 문장 더 → **이도저도 아니다**. 대비로 답을 닫아 봤지만 과제는 여전히 *한국어 → 영어 한 문장*이었다
    - 상대가 말을 걸면 그 표현으로 답하기를 2~3번 → **개수만 늘고 사실상 같다**. 각 라운드가 독립적인 번역 과제면 반복은 반복이 아니다
    - 표현을 먼저 묻고 통과해야 재료를 주는 두 박자 → `어떤 표현을 쓸까요?`는 학습 행위가 아니라 **진행자의 말**이라 억지스럽다
    - 칸마다 첫 글자만 남기기 → **앱에 없는 표기**라 낯설다
    - 내용을 자유롭게 두고 **영어 문장 전체의 적법성까지 채점하려 하기** → 정답 집합이 열려 **실시간 LLM 채점이 필요해진다**(비용상 불가). `o`는 채점을 **프레임까지만** 걸어 이 비용을 비켜 가고, 틀린 영어가 통과할 수 있는 것을 상한으로 받아들인다
    - **후보 뱅크를 주고 그중 아무거나 골라 쓰면 다 맞게 하기**(`자리 갈아 끼우기`) → **그냥 가져다 쓰는 것**이라 효용이 없다. 답을 여럿으로 열어도 그 여럿이 화면에 적혀 있으면 조립이다. 열린 자리에 넣을 수 있는 말을 뱅크로 닫아 주면 LLM 채점 없이 답이 여러 개가 되지만, 정작 *뭐든 넣을 수 있다*가 목록 읽기로 바뀐다
    - **연습지 마지막 줄에 앞서 배운 다른 표현을 섞기**(`복습 섞기`) → 축이 하나 더 늘 뿐 이 단계의 과제를 바꾸지 않는다. 복습 주기는 `[4]` 안에서가 아니라 **1~3일 뒤 함정을 섞은 재노출**에서 판정하는 것이 설계다(그 단계는 아직 없다)
    - 재료를 탭해 넣고 표현만 치기 / 뱅크에 빈 카드를 섞어 그 자리에 쓰기 → **조작이 섞여 애매하다**. 탭으로 배열하다 갑자기 타이핑으로 넘어가면 어느 쪽 과제인지 흐려지고, 빈 카드를 배열한다는 것 자체가 읽히지 않는다
    - 이 표현으로 할 말을 한국어 2~3개 중에 고르고 쓰기 → **굳이 하나를 골라야 하는 군더더기**다. 고르기는 과제가 아니라 과제 앞의 절차라, 단계만 늘고 얻는 것이 없다
    - 표현 철자를 표제에 걸어 두기 / 첫 줄에 모범 답안을 채워 두기 → **보고 옮겨 적는 일이 된다**. 답이 화면에 있으면 남는 조작은 조립뿐이라, 타이핑을 시켜도 산출이 아니다
    - 대화문에서 **앞 대사만** 주기 → **사실상 `영어 질문`과 같다**. 내 말 뒤가 비어 있으면 대화의 한가운데가 아니라 질문에 답하는 자리다
    - 자극과 방식을 쪼개지 않고 **일곱 방식을 나란히 늘어놓기** → **각각 무슨 차이가 있는지 모르겠다**. 절반이 *한국어 뜻 → 영어 문장*에 껍데기만 바꿔 끼운 것이라 실제로 다른 축은 둘(자극·재료)뿐이었다
    **채점은 모범 답안과의 정확일치가 아니다** — 같은 한국어를 옮기는 영어는 여러 가지라 정확일치만 받으면 맞는 답을 틀렸다고 하게 된다. 조건은 두 개뿐이고 모든 방식이 같은 것을 쓴다: ① **배운 표현을 썼는가**(`framePos`, 조각 순서·굴절 허용) ② **주어진 낱말로만 만들었는가**(프레임+주어진 낱말의 다중집합 일치, `spkMatch`). 통과하면 어순이 달라도 맞고, 모범 답안과 다르면 `ⓘ 이렇게도 말해요`로 함께 보여준다. 재료를 주지 않는 칸에서는 ②가 빠지고 대신 `표현 밖 낱말 1개 이상`·`중복 아님`이 들어간다.
    **오타 한 글자는 같은 낱말로 본다**(`nearWord`·`editDist`) — 이 단계의 과제는 표현을 쓸 수 있느냐지 철자를 정확히 치느냐가 아니다. 화면에 `This`가 보이는데 `Thsi`로 쳤다고 `thsi 는 이 문장에 필요하지 않아요`라고 하면 **아는 답이 막힌다**. 거리는 **Damerau**를 쓴다 — 바꿔치기(`thsi`↔`this`, `form`↔`from`)가 가장 흔한데 Levenshtein으로는 2가 된다. **4글자 이상에서만** 봐준다: 짧은 낱말은 한 글자만 달라도 다른 낱말인 경우가 많고(`she`/`the`, `too`/`to`, `on`/`in`), 흔한 오타는 어차피 4글자 이상이다. 짝짓기는 **정확히 같은 것부터 소진한 뒤 남은 것만** 오타로 본다 — 순서를 바꾸면 멀쩡한 낱말이 오타로 잡힌다. 오타로 통과하면 **정답 철자로 굳히고**(내가 만든 문장으로 남는 것이라 틀린 철자를 남기면 안 된다) `철자 thsi → This` 한 줄로 짚어 준다.
    막힐 때는 무엇이 어긋났는지 짚는다 — 표현을 아직 안 썼으면 **지적하는 대신 오답노트 팁을 연다**(`[2]`와 같은 규약), 재료가 어긋났으면 `<낱말> 는 이 문장에 필요하지 않아요` / `<낱말> 를 아직 안 썼어요`.
    **효용감이 나려면 표현이 내 손에서 나와야 한다** — 표현을 미리 박아 두고 슬롯만 채우게 하면 *내가 이 표현을 썼다*가 되지 않는다(빈칸 하나 채우기를 검토했다가 이 이유로 접었다). 그렇다고 문장 전체를 새로 만들게 하면 어렵다. 이 둘이 충돌하는 것처럼 보이지만, **어려움의 원인은 '전체'가 아니라 문장의 길이와 새로움**이다: `Don't be late.` → `Don't be sad.` 라면 표현도 내가 쓰면서 4낱말이라 부담이 없다. **틀은 그대로 두고 낱말만 바뀌는 짝**이 이 단계가 노리는 크기다.
    **대상 문장은 [2]·[3]에서 만든 문장이 아니라 새 문장**(`speakTarget`)이다. 앞에서 쓴 문장을 다시 쓰면 표현을 내가 써먹는다는 효용이 나오지 않는다. 대상 문장은 소스 CSV의 형제 문장(`apply[]`)을 그대로 쓰므로 **무엇을 고르느냐가 곧 난이도**다 — 방금 배운 표현인데 문장 구조까지 새것이면 표현을 쓰는 과제가 아니라 새 문법을 푸는 과제가 된다(`Take off your sweater.`를 배우고 `Should I take off my shoes?`가 나오면 평서문 → 의문문 도치까지 얹힌다). `How about a lemonade?` → `How about Thursday?`처럼 **틀은 그대로 두고 단어만 바뀌는 것**이 맞는 난이도다. 그래서 순서대로 고른다:
    ⓞ **자극이 A 대사를 쓰면(`en`·`dlg`) 대사가 있는 후보를 먼저 남긴다** — 대사가 없는 문장이 대상이 되면 그 칸의 화면 자체가 성립하지 않는다. 그 다음 규칙은 그대로 적용된다.
    ① **틀은 그대로 두고 낱말만 바뀌는 짝**이 있으면 무조건 그것을 쓴다 — 넷을 모두 만족할 때만 인정한다(하나라도 어긋나면 난이도가 튄다): 재료 2낱말 이하 · 문장 5낱말 이하 · 조각 구조 일치 · 종결부호 일치. 데이터에 그런 짝이 있는 문항은 **62/261**이고, 나머지는 아래 규칙으로 고른다. **그 62문항만 모아 푸는 진입점을 홈에 둔다**(`startTwinOnly`) — 크기가 맞는지 바로 확인하기 위해서다. **단계를 건너뛰지 않는다**: 효용감은 `[2]`·`[3]`에서 그 표현을 방금 만들어 본 뒤에 오는 것이라 `[4]`만 떼어 보면 판단이 어긋난다.
    ② 학습 문장의 **부분/포함 문장 제외** — 새로 세울 것이 없다(현재 0건).
    ③ **조각 구조가 같은 것**(`F,S` ↔ `F,S`) — 난이도를 가르는 가장 큰 축(260/261에서 가능).
    ④ **종결부호가 같은 것** — 평서문/의문문이 갈리면 구조가 멀어진다(260/261 일치).
    ⑤ **재료가 적은 것 → 짧은 것** — 재료가 많으면 재료로 문장을 조립하는 일이 되고 표현이 그 안에 묻힌다(`a lot of`를 배우고 `You ask a lot of questions.`).
    **대상이 바뀌면 `en`·`dlg`의 대사도 새로 필요하다** — 대사 파일은 영어 문장을 키로 하므로 바뀐 대상만큼 추가 저작한다.
    **정답 후 결과창은 효용감을 문구로 말하지 않는다** — 같은 문장을 라벨 달아 한 번 더 늘어놓는 것은 설명이지 근거가 아니다. 대신 입력칸을 완성 문장으로 굳히고(`.sp-tiles`, 프레임만 초록) 그 아래 `학습 정보`에 **이 표현으로 만든 문장들**을 나란히 둔다. **같은 초록 조각이 여러 문장에 반복되는 것**이 "이 표현을 내가 썼다"의 근거다([3] 결과창과 같은 원리). 대상 문장이 정해진 방식은 두 줄(학습 문장 + 방금 만든 문장)이고, `o`는 앱이 아는 문장을 전부 세워 내 문장을 그 첫 줄로 끼운다. 모범 답안과 다르게 통과했으면 `ⓘ 이렇게도 말해요`를 한 줄 더 붙인다.
    `frameLine`이 문장에서 프레임만 초록으로 칠한다 — `segs`가 있으면 조각째로, 유저가 직접 쓴 문장은 프레임 조각을 정규식으로 찾아 감싼다(굴절은 `s`/`es`/`ed`/`d`/`ing`까지). **`siblings[]`는 조각 정보가 없어 철자·인칭이 바뀌는 활용은 초록이 안 붙는다**(`He took off his sweater.`·`What was going on in London?`, `o` 결과창 기준 46/1775줄 = 2.6%). 조각을 공백으로 이으면 `, grandpa` 같은 슬롯에서 `time , grandpa`가 되므로 **부호 앞 공백을 지운다**.
  - **[5] 상황(`renderSituation`)** — 기획 의도는 "임의의 상황에서 배운 표현을 가지고 내가 새로 말을 할 수 있다"이고, 학습 방식은 **영어를 보고 영어로 인출**이다. 그래서 푸는 동안 한국어 해석을 노출하지 않고, A 대사 해석(`#aKr`)과 내 답의 뜻은 **답한 뒤에만** 연다.
    **응답은 2~3개 중에서 고른다**(듀오링고 `~의 말을 듣고 적절히 답변하세요` 형태). 선택지는 전부 직전에 배운 문장이고 **오답은 문법은 맞지만 상황에 안 맞는** 문장이라 과제가 **문맥 적합성 판단**이 된다. 오답 후보는 **같은 구간(또는 인접 문항)의 다른 표현 문장**에서 뽑으므로 새 데이터가 필요 없다. 지시문은 한국어지만 generic 하다(`A의 말을 보고 적절히 답변하세요`) — 정답의 번역이 아니다. 오답을 고르면 그 선택지만 붉게 표시하고 A 해석은 계속 잠긴다. 정답이면 `✓ 좋아요!` + `의미`(내 답의 한국어) + `ⓘ 쓴 표현`(프레임·뜻)을 열고 정답 선택지를 초록으로 표시한다(비교는 `data-ok`로 — 마이크 아이콘이 섞인 `textContent`로 비교하면 빗나간다).
    **선택지를 표현 칩으로 바꿔 고르게 한 버전은 되돌렸다** — 읽는 부담은 줄지만 정작 '말한다'는 감각이 옅어졌다. 이 단계에 남아야 하는 산출물은 **완성된 한 문장**이다.
    **자유 입력 버전도 나란히 둔다** — 아무 단서 없이 쓰게 하면 막히고 다 주면 인출이 아니므로 **지원을 한 칸씩만 여는 힌트 사다리**를 쓴다: `배운 표현 목록 → 이 상황의 표현 하나 → 첫 단어 → 정답`. 채점은 프레임 포함 여부로만 하므로 모범 답안과 다른 문장(`Hey, you should get up already!`)도 통과한다. 그때 `의미`는 **내가 말한 문장의 뜻**이어야 하므로 모범 답안의 한국어를 거기 붙이지 않고 `ⓘ 이 상황에선 이렇게 말해요`로 따로 뗀다.
    두 방식은 홈 `상황만 풀기` 카드의 **`골라 답하기` / `직접 입력` 두 진입점**으로 각각 들어간다(세션 중 [5]는 항상 선택형). **[5]는 3문항마다 나오므로 한참 풀어야 볼 수 있다 — 홈에 `상황만 풀기` 별도 진입점**(`startSituationOnly`)을 둬 상황이 붙은 문항만 연속으로 풀 수 있다. 이 모드에선 `sitOnly=true`이고 `buildStepIndex`가 문항당 1단계로 계산한다. **개발 확인용으로 `devPanel`이 카드 아래 붙어** 이 상황이 어떤 표현으로 만들어졌고 오답이 어디서 왔는지 보여준다(점선·모노 표기로 학습 UI 와 구분, `sitOnly`에서만).
    **A 대사는 소스 CSV에 없어 `scripts/vocab_expression_situations.json`에 손으로 적었다**(학습 문장 `sentence.en`을 키로 하는 `{a_en, a_kr, hint_en}`, 현재 30개). 빌드가 `situation`으로 붙이고, **구간에 상황이 붙은 문항이 없으면 이 단계를 건너뛴다**.
  - 종결부호는 flex 항목이라 그냥 두면 **부호만 다음 줄로** 떨어진다 — `withPunct`가 마지막 칸과 묶어(`.fill-nw`) 함께 넘긴다.
  - 배열 화면은 **한국어 뜻 + 빈 줄**만 둔다 — 앱 화면에 없는 안내 박스는 두지 않고, 문장 배열에는 지시문도 두지 않는다(`문장을 만들어 주세요`는 뜻과 빈 줄만으로 자명하다). 흐름 1의 1박자에만 `표현을 먼저 만들어 보세요`를 남긴다 — 거기서는 문장이 아니라 표현만 세우는 것이 화면만으로 읽히지 않는다. 상황 태그(일상/여행/비즈니스)와 활용 규모 수치(`N가지`)도 **쓰지 않는다** — 태그는 콘텐츠에서 없어질 수 있고, 수치는 실제 용례가 더 많을 수 있어 의미가 없다. **영어 프레임(`pattern.form`)은 정답 어순이 새므로 스스로 풀고 있는 동안에는 노출하지 않는다** — 정답 후 `학습 정보`에서 공개하고, 1박자에서 표현을 못 떠올렸을 때만 오답노트 팁으로 먼저 보여준다(아래).
  - **결과창(`patternPayoffHtml`)** — `학습 정보` + `💡 새로운 표현을 배웠어요` + **재사용 프레임 공개**(`pattern.form`의 `______`/`~`를 빈칸으로 그린 초록 큰 글씨 + `pattern.meaning` 회색 뜻) + `pattern.desc`가 있으면 회색 박스 한 줄 + `ⓘ 단어만 바꿔서 이렇게도 말할 수 있어요` + **응용 예문 2개**(`siblings`, 구분 없이 전부 초록 — 내부자 테스트 기준). `학습 정보`는 문제 카드 **밖의 별도 카드**다(`.payoff`, 좌상단 탭 라벨) — 문제 카드는 방금 푼 문장의 것이고 학습 정보는 그 문장이 아니라 **표현**의 것이라 소유가 다르다.
    **정답 후 하단은 다음으로 가는 버튼 하나**다(`arrangeBottomHtml`) — 여러 버튼을 늘어놓으면 무엇이 다음 단계인지가 흐려진다. `다시 듣기`·`다시 말하기`처럼 **방금 푼 문장에 거는** 동작은 그 문장이 있는 카드 안 점선 아래 액션 줄로 내린다(`resultActionsHtml`, 비작동 표시).
  - **짧고 일반적인 클래스명을 새로 만들 때 기존 정의와 겹치는지 먼저 확인할 것**(`.done`·`.sw` 둘 다 이 방식으로 깨졌다).
  - 데이터 스키마: `items[]` = `{trigger,word,meaning,pos,cefr,level,rank, vocabA{answer,enLines,koLines}, vocabB{answer,enLines,koLines,hint}, sentence{en,kr,trigger}, pattern{form,meaning,desc}, frame[리터럴 조각], traps[함정 단어], baseSegs[{t,s}], apply[{en,kr,punct,segs[{t,s}]}], siblings[{en,kr}], ask{en,kr}, situation?}`. **`traps`는 현재 프로토타입 화면에서 쓰지 않는다** — 함정은 1~3일 뒤 복습 판정용이고 그 단계가 아직 없다. 데이터는 그대로 굽는다. TTS·음성 등 앱 부가기능은 시각적으로만 재현(비작동), 리딩·타이핑 중심.
- `public/listening-pingpong.html` — **별도** 리스닝 핑퐁 프로토타입(자체 완결형 HTML + 브라우저 TTS, 본 앱과 무관, `/listening-pingpong.html`로 서빙). 콘텐츠는 외부 JSON에서 로드한다. **v1(`DATA_GROUPS`)은 현재 AI 생성 리스닝 데이터 검토용 샘플**(`listening-pingpong.data.rv-a1/-a2/-b1.json`, 레벨 A1/A2/B1별 ~15문항, `scripts/lp_csv_to_data.py`가 대화 CSV + 표현 CSV를 **id로 조인**해 생성)을 **레벨 그룹**으로 보여준다. 큐레이션 데이터(`listening-pingpong.data.json` 초급 A1-A2 + `.b.json` 중급 B1-B2)는 v2/v3/r1이 쓴다. 완료한 대화는 `localStorage`(`lp:cleared`)에 저장해 ✓ 완료 배지·`n/N 완료` 카운트로 표시(하나씩 클리어). 흐름은 **표현(단어 배열) → 리스닝**: A(상대)가 트리거 문장을 말하고 B(학습자)가 3지선다로 답하는 오청(mishearing) 함정 퀴즈. A 대사는 텍스트 없이 음성만(시작 전 3·2·1 카운트다운), 답하면 각 선택지의 한국어 뜻·해설을 보여준다. **답한 뒤**에는 그 A 말풍선을 탭해 영어 대사를 펼쳐 볼 수 있다(답하기 전엔 잠금 — 훈련 훼손 방지, `setupPeek`/`unlockA`). 홈의 **'A 노출' 옵션**(`lp:expose`)으로 A 대사 방식을 고른다: `듣기`(음성) 또는 `고정·읽기`(음성 없이 단어를 하나씩 제자리 교체하는 RSVP, `rsvpReveal`) — 리딩은 **대화 영역(chat) 위에만 겹치는 중앙 오버레이**로, 뒤 대화를 흐리게(blur) 덮고 상단 진행바는 유지하며 부드럽게 페이드 인/아웃한다(화면 통째 교체 금지 — 되돌아올 때 끊김 방지, `chat.getBoundingClientRect()`로 위치·크기 지정). 고정 모드에선 **속도 설정이 RSVP 속도를 겸하고**(음절수×280/rate), 준비 문구·다시 보기가 모두 '보기'로 바뀐다. **틀리면** A 대사를 다시 접하고 그 문장을 단어 배열로 재구성하는 교정 단계가 뜬다(`buildArrange`) — 듣기는 오답 뒤 음성을 자동 재생하지만, **고정은 갑자기 뜨지 않도록 '👀 다시 보기'를 눌러야** 리딩이 노출된다(`makeRsvpPlayer`, 자동 재생 없음). 표현이 나온 트리거 문제에서는 정답 후 "🎧 방금 A가 이렇게 말했어요 + 그 문장 + 배운 표현" 박스로 연결해준다. done 화면에서 전체 스크립트(영어+한국어)를 볼 수 있다. 데이터 스키마: `examples[]`에 `vocab`·`expression{csv_id?,en,kr,arrange?{en,ko}}`·`noArrange?`·`dialogue[{role,text,kr}|{role:'B',choices:[{text,kr,correct,trap,explain,highlight}]}]`; `expression.arrange`가 있으면 그 문장을 '표현 배열' 대상으로 쓰고(리스닝 대사와 달라 스포일 없음), 없으면 표현에 `~`가 있을 때 트리거 A 대사에서 자동 생성한다. `noArrange:true`면 표현 단계를 건너뛰고 바로 리스닝으로 간다(표현 조인 실패 시 fallback). 큐레이션 표현은 실제 표현 뱅크(ExpressionUnitKR) 항목(csv_id=seq). 강조(형광펜/밑줄)는 현재 쓰지 않음. A/B는 서로 다른 음성(여성/남성 우선)으로 재생하며 홈에서 각각 고른다.
- `public/listening-pingpong-v2.html` — 리스닝 핑퐁 **UI v2**(`/listening-pingpong-v2.html`). 데이터 JSON과 `lp:*` localStorage는 v1과 공유하되 UI를 재구성: **표현(단어 배열) 선행 단계 제거**(홈 → 바로 리스닝), 화면은 **상단 문제 카드(레벨 헤더 + 대화) + 하단 답안 영역** 2단. 문제 전환은 **스왑 체인(크기 2)** — 결과를 보여주는 동안 `back` 버퍼(`visibility:hidden`이라 레이아웃·스크롤 유지)에 다음 문제 화면을 미리 렌더해 두고 '다음'에서 교체(`swapScreens`, 슬라이드 인). 각 문제 화면은 매번 새로 만드는 별개 컴포넌트고, 이전 대화 내역은 DOM 복제가 아니라 **turns 데이터에서 재렌더**(`newChatScreen`) — 위로 스크롤해 열람하되 손을 떼면 현재 문답으로 복귀(sticky, `attachSnapBack`). 답변 흐름: 답하면 B 정답 대사가 바로 말풍선으로 보이고 💡 해설(정답 초록/오답 노랑, 오답이면 고른 문장 + 수평선)이 카드에 남는다. **정답 → A 말풍선 탭 열람 허용 후 다음 문답 / 오답 → A 대사 계속 숨긴 채 문장 배열 교정 문제**(카드에 뜻·플레이어·배열 줄, 답안 영역에 단어 뱅크). 결과 영역의 **오답 노트** 버튼은 모범답안 + 오답 이유 팝업(`showWrongNote`). A 말풍선은 재생 전/후에도 정적 웨이브 유지(크기 고정).
- `public/listening-pingpong-v3.html` — 리스닝 핑퐁 **UI v3**(`/listening-pingpong-v3.html`). 데이터 JSON·`lp:*` localStorage는 v1/v2와 공유. v2의 문제 카드 안에서 **대화가 v1처럼 메신저식으로 아래로 누적**된다(스왑 체인·재렌더 없음). 채팅은 일반 column 스크롤러 + **JS 하단 고정**(`pinBottom` — 패널이 오르내리는 동안 rAF로 매 프레임 바닥 고정; column-reverse 네이티브 고정은 iOS Safari의 시작 방향 overflow 접근 불가 WebKit 버그로 깨져서 쓰지 않는다) — 그래서 **선택지 패널이 카드 하단에서 키보드처럼 올라올 때 대화가 위로 밀리고, 내려갈 때 함께 내려온다**(패널은 `grid-template-rows 0fr→1fr` 애니메이션, `raisePanel`/`lowerPanel`). 답하면 각 선택지에 뜻·해설이 **인라인** 표시(v1 방식, 별도 오답 노트 팝업 없음)되고 B 정답 말풍선이 채팅에 쌓이며, **'다음'을 눌러야 패널이 내려간다**. **오답 교정(문장 배열)은 화면 교체 대신 바텀 시트**(`fixSheet` — 카드 톤의 안내·뜻·플레이어·배열 줄 + 아래 단어 뱅크)가 올라와 풀고 나면 내려간다(뒤의 대화 유지). A 말풍선 열람은 정답 시 즉시, 오답 시 배열 완료 후 허용. **다시 듣기는 A 말풍선 안 스피커 버튼**(구분선 옆 본문은 웨이브 → '영어 보기' → 영어 전문으로 바뀜, 각 말풍선이 자기 대사를 재생) — 카드 하단 버튼 없음.
- `public/listening-pingpong-r1.html` — v3의 레이아웃 변형(`/listening-pingpong-r1.html`). 선택지 패널을 **카드 밖(아래)으로 분리**하고 `min-height:40vh`로 하단 몫을 상시 예약해 **카드 최대 크기를 v2 수준으로 고정**(기본 선택지 3개는 예약분 안이라 첫 등장 시 카드 불변). 패널은 **바텀시트처럼 아래에서 슬라이드 인/아웃**(transform, 내려간 상태는 overflow:hidden으로 클립, 내용은 박스 하단에 붙음). 해설 등으로 내용이 예약분을 넘으면 카드가 그만큼 줄고, '다음'으로 내려간 뒤 내용을 비울 때는 **height 트랜지션으로 원래 크기 복귀**(툭 튀는 것 방지). 답변 직후 A 문장은 기본으로 펼쳐짐(탭으로 숨김 토글). 그 외 흐름·데이터는 v3와 동일.

화면 흐름: 스토리 선택 → 에피소드(토픽) 목록 → 시작 화면(모드 선택) → 세션(리딩/리스닝 재생) → 퀴즈(바텀시트). `App.tsx`가 상태기계로 관리(`mode`·`playing` 상태).

## 데이터 파이프라인

`dialogs.json`은 커밋된 생성물이자 런타임의 유일한 소스다(sqlite 없이도 앱은 동작). 재생성할 때만 `scripts/extract.py`를 쓰고, **손으로 편집하지 말 것**.

- 출처: bi의 `server.sqlite` (이 레포 밖). 경로는 `READING_POC_SQLITE` 환경변수 또는 CLI 인자로 지정. 없으면 명확한 에러로 종료한다.
  - 필수 테이블: `ConversationDialogScriptKR`(스크립트 + `voiceInfos` 해시), `ResourceFileKR`의 `ConversationCourses`(코스/토픽 + 대화별 `di[].h` = dialogHash). 사전 전용 오래된 export에는 대화 테이블이 없으니 **최신 export를 쓸 것**.
- 포함 코스: `INCLUDE = [2(뉴욕여행·추천), 25(잡스), 17(투자)]`(배열 순서 = 화면 표시 순서), 레벨 A2(=2) 고정. 첫 코스(2)는 `StoryList`에서 "추천" 배지.
- 각 에피소드는 **도입부 4턴(`MAX_SCRIPTS`)만** 사용 — 리딩 분량을 짧게.
- 단어 경계는 tagList가 아니라 문장 문자열을 직접 파싱(문장부호 보존).
- **리스닝 오디오 URL**을 스크립트마다 `audioUrl`로 구워 넣는다. 형식:
  `https://speech.epop.ai/conversation/tts/{dialogSeq:06d}-{dialogHash}/{voiceType}/{scriptIdx:02d}.{voiceHash}.ogg`
  (dialogSeq = topicSeq×10+level, scriptIdx = scriptSeq%100, voiceType = `voiceInfos`의 non-null 인덱스 폴더명, voiceHash = 그 해시). CDN은 인증 없는 공개 접근이라 런타임에서 바로 재생된다.

`quizzes.json`은 `topicSeq`(코스별로 유니크)를 키로 하는 수기 저작 데이터. 대화당 1문항.

## Conventions

- 주석·UI 문구는 한국어. 식별자만 영어.
- 순수 로직(`chunk.ts`, `reveal.ts`, hook)은 TDD. `src/**/*.test.ts(x)`.
- 생성물 fixture(`dialogs.json`)도 커밋한다(런타임 sqlite 의존 0).
- 음절 계산은 `syllable` 패키지.

## Quirks / 주의

- **노출 규칙**: 청크 i는 (i+N)번째 청크가 등장하는 순간 사라진다(항상 최근 N개 유지). 문장 끝의 마지막 N개는 등장할 다음 청크가 없으므로, 평균 dwell 간격으로 하나씩 빠지게 스케줄을 이어붙인다(그러지 않으면 마지막 창이 동시에 사라져 끝 청크를 볼 시간이 부족 — `buildRevealSchedule`의 `showAtExt`, 간격 상한 `DRAIN_STEP_MS`). dwell = `max(100ms, 음절수 × 속도설정)`(하한이 높으면 짧은 단어가 바닥에 걸려 속도 설정이 안 먹히므로 `MIN_DWELL_MS`는 낮게 유지). 기본은 opacity 페이드(등장 450ms, 사라짐은 250ms로 더 빠르게, 자리는 유지)이며 등장/사라짐을 각각 "페이드 인"(`fadeIn`)·"페이드 아웃"(`fadeOut`)으로 끌 수 있다. transition은 `DialogBubble`이 방향별로 인라인 지정한다(값이 바뀌는 순간의 transition이 등장/사라짐을 결정 — 보이면 fadeIn, 숨기면 fadeOut). "오래된 청크 숨기기"를 끄면(`hideOld=false`) 창 크기를 무시하고 문장 끝까지 누적한다. 상세는 `reveal.ts`.
- **퀴즈 정답은 반드시 노출되는 턴(현재 4턴) 안에 있어야 한다.** `MAX_SCRIPTS`를 줄이면 정답이 잘린 문제가 생기므로 해당 퀴즈를 재작성해야 한다. 재생성 후 `dialogs.json`에 정답 근거 문구가 있는지 확인할 것.
- **localStorage 키**: `reading-poc:settings`(설정) · `reading-poc:solved`(푼 토픽). 기존 저장값이 있으면 기본값보다 우선하므로, 새 기본값 확인 시 저장소를 비운다.
- **모드 선택·다시 듣기**: 토픽 진입 시 `SessionStart`(모드 선택 + 리딩 설정)를 거쳐 재생. 리딩 노출 설정은 리딩 모드에서만 노출. 퀴즈의 "다시 듣기"는 시작 화면을 건너뛰고 **같은 모드로 즉시** 처음부터 재생(`sessionRun` 증가로 세션 리마운트).
- **리스닝 재생**: 문장 단위 오디오를 순차 재생, `ended`(또는 `error`)에 다음으로. 자동재생은 시작 버튼 클릭(사용자 제스처) 직후라 허용된다. 텍스트는 숨기고 재생 중 말풍선에만 웨이브 인디케이터를 표시.
- **고정 재생**: `FixedSession`이 화면 중앙 한 곳에서 단어/청크를 제자리 교체(RSVP). 창·페이드·hideOld 등은 무관하고 **노출 단위(단어/청크)와 속도(ms/음절)만** 따른다(dwell = `max(100ms, 음절수×속도)`, `reveal.ts`의 `dwellMs` 재사용). 대화 맥락은 현재 화자(A/B)·진행바로 유지. 시작 화면에서 고정 모드는 단위·속도만 조절.
- **전광판 재생**: 시작 화면에서 하위 방식(`marqueeStyle`)을 고른다. `MarqueeSession`은 진입점으로 방식에 따라 분기.
  - **문장별**(`sentence`, 기본): 각 말풍선(`MarqueeSentenceSession`)이 자기 텍스트를 Web Animations API로 오른쪽→왼쪽 1회 흘려보낸다(`overflow:hidden` 레인, 흐름 뒤엔 빈 말풍선). 다음 말풍선은 현재 문장이 화면에 다 들어온 시점에 **겹쳐서** 등장해 풀-스톱 없이 연속 흐른다.
  - **한 줄**(`stream`, `MarqueeStreamSession`): 고정된 한 줄 레인에서 문장 전체가 오른쪽→왼쪽으로 연속으로 흐른다(여러 단어가 이어서 보임). 화자(A/B)·진행바로 맥락 유지, 문장 단위로 순차 진행.
  - 속도는 공통 `marqueeSpeed`(px/초, 40~400).
- **Vercel 배포**: 커밋 author 이메일이 GitHub 계정(namda-on)에 등록된 주소여야 배포가 식별된다. 이 레포는 `user.email = namda1571@gmail.com`으로 설정돼 있다.
- **화자 표시는 A/B**, 에피소드 제목은 "Episode N"만 — 제목·상대역이 주제를 노출해 문제가 쉬워지는 것을 막기 위함.
- **리딩 시작 준비 신호**: 곧바로 노출되면 당황스러우므로, 첫 텍스트 자리에 dot 을 `READY_MS`(0.8s) 동안 깜빡인 뒤 재생을 시작한다(`ready` 게이트).
- **리딩 옵션 프리셋**: `SettingsPanel`은 프리셋 3개(단어 / 청크 / 누적)를 원클릭 제공하고, "직접 설정"을 펼치면 개별 옵션을 조절한다(옵션 설명은 그 안 맨 아래에 표시). 프리셋은 노출 관련 필드(단위·창·속도·hideOld·최대청크)와 **페이드 인/아웃**을 지정한다(청크 프리셋은 창2 — 다음 청크 등장 시 이전 청크가 밀려나는 슬라이딩이 보이도록, 청크·누적은 페이드 인 켜기가 기본). **끝 사라짐 간격·전광판 속도는 프리셋과 독립**(프리셋 클릭 시 유지, `matchesPreset` 판정에서도 제외). 현재 설정이 프리셋과 일치하면 하이라이트, 아니면 "커스텀" 배지.
- **설정 지속**: 설정은 `reading-poc:settings`에 저장돼 마지막 상태가 유지된다(진입 시 리셋하지 않음). 마지막에 고른 모드(`reading-poc:mode`)와 문제 유형(`reading-poc:quizType`)도 저장돼 다음 토픽 진입 시 기본 선택된다(`App`이 저장, `SessionStart`의 `initialMode`·`initialQuizType`). 기본값(`DEFAULT_SETTINGS`)은 단어·창4·속도300·hideOld켜기·**페이드 인 끄기/아웃 켜기**.
- **문제 유형**: 세션 후 문제를 시작 화면에서 고른다 — `comprehension`(객관식 이해, `quizzes.json`), `arrange`(한국어 뜻→영어 단어 순서 맞추기), `dictation`(오디오 듣고 타이핑, 대소문자·문장부호 무시 채점). 배열·받아쓰기는 `pickKeySentence`로 고른 **단일** 핵심 문장(퀴즈 정답 근거 문장)을 대상으로 하며 기존 데이터(english·translated·audioUrl)만 사용. 마지막 유형은 `reading-poc:quizType`에 저장. 다시 풀 때 유형을 바꿔 지루함을 줄인다.
- **문제 미리보기**: 이해(`comprehension`) 유형일 때만 세션 상단에 `QuestionBanner`로 질문을 처음부터 노출한다(`App`이 `showQuestion`으로 전달; 배열·받아쓰기는 어떤 문장이 나올지가 문제라 숨김). 질문은 `quizzes.json`에서 `topicSeq`로 읽고, 선택지는 끝난 뒤 `QuizSheet`에서.
- **퀴즈 보기 셔플**: 저작 데이터(`quizzes.json`)가 정답을 대부분 1번(`answerIndex:0`)에 두어 위치로 답을 유추할 수 있으므로, `Quiz`가 마운트 시 보기를 1회 셔플한다(정답 위치 균등 분산). 데이터의 `answerIndex`는 그대로 두고 런타임에서만 섞는다.
